## 1. CI deployment

- [ ] 1.1 deploy-website.yml (push paths + dispatch; filtered install →
  build → shape checks → wrangler pages deploy via secrets).
- [ ] 1.2 Verify locally: filtered install + build green (the deploy step
  runs in CI with secrets).
- [ ] 1.3 Merge → first CI deploy green → live site serves the merged
  registry-adoption + mobile + consumer-upgrade content.

## 2. Wrap

- [ ] 2.1 NOTES/README deployment section updated; friction log reported.
