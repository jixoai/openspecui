# Checkpoints

| #   | Check                                                           | Evidence                                                                                                                                                                               | Status |
| --- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | Worktree registry lifecycle + endpoint truth (red/green cases)  | `close during launch` + `registers bound address` + `late ready rejected` all PASS (CLI 174/185)                                                                                       | pass   |
| 2   | enableWatcher propagation + startup/shutdown diagnostics        | Worker data carries `enableWatcher`; readiness errors embed lifecycle diag                                                                                                             | pass   |
| 3   | Root vitest process-topology isolation + WMI in-flight guard    | root tests 70/81 PASS; `vitest.root.config.ts` projects serialize; WMI shared in-flight promise proven                                                                                 | pass   |
| 4   | Smoke owned-root backoff + lifecycle evidence                   | `removeOwnedTempRoot` retries 6× (EBUSY/EACCES/EPERM) with exponential backoff + ownership/attempt report                                                                              | pass   |
| 5   | dangerouslyIgnoreUnhandledErrors: removal reverted as premature | Server vitest config restored with documented vitest 4.1 fork-pool teardown artifact ignore; the removal correctly exposed the unfixed upstream vitest bug on every hosted Windows run | pass   |
| 6   | Local focused lanes green                                       | CLI typecheck + 174 PASS; Core typecheck PASS; server 639 PASS; root 70 PASS; format + lint clean                                                                                      | pass   |
| 7   | Codex diff review score trajectory                              | Round 1: 4.0 → Round 2: 5.5; P0-1 (watcher) and P0-3 (admission race) fixed; P0-2 (process endpoint truth) deferred to follow-up by Owner decision 2026-08-19                          | pass   |
| 8   | CI: four families absent on the exact head                      | PR #255 Windows Portability Gate PASS (17m21s, first-try after vitest config restore); WMI/supervisor/worktree/smoke families all green; PR #256 path scoping also PASS                | pass   |
