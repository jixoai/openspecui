<!--
Orthogonal intents (created 2026-08-28 Asia/Shanghai):
1. Generalize the version-bound static projection law to the current release line.
2. Pin generator-evidence truth for the 1.10/1.11 admission range.

Original request (2026-08-28): "我们直接将 0.10.0 和 0.11.0 一起适配，然后发布 v11"
-->

## RENAMED Requirements

- FROM: `### Requirement: Static Projection Does Not Invent v9 Runtime Evidence`
- TO: `### Requirement: Static Projection Does Not Invent Line-Current Runtime Evidence`

## MODIFIED Requirements

### Requirement: Static Projection Does Not Invent Line-Current Runtime Evidence

Static exports MAY include captured OpenSpecUI-11 compatibility or policy facts but SHALL NOT synthesize live
Schema resolution, archived validation, archive retirement, requirement diffs, batch status, or global Agent
filesystem observations.

#### Scenario: Static archive evidence is absent

- **GIVEN** a static export contains no captured archived-validation result
- **WHEN** Change Evidence renders
- **THEN** it SHALL identify the evidence as unavailable in the static snapshot
- **AND** SHALL NOT display a fabricated pass, failure, root, or Agent repair state

#### Scenario: Static diff evidence is absent

- **GIVEN** a static export contains no captured requirement-diff payload
- **WHEN** Change Detail renders a MODIFIED delta
- **THEN** the delta SHALL present its captured local content only
- **AND** SHALL NOT display a locally recomputed diff or a fabricated upstream warning

## ADDED Requirements

### Requirement: Generator Evidence Follows the Admitted CLI

Agent generator evidence SHALL resolve the running OpenSpec CLI's live version and SHALL compare on-disk
generated state against the admitted 1.10/1.11 expectations. The pinned generator constant SHALL state the
1.11.0 baseline and SHALL never be substituted for a live resolution. Generated-by comparisons SHALL be
series-aware: artifacts generated by either admitted series SHALL NOT be flagged stale merely for differing
from the other admitted series or from the pinned baseline constant.

#### Scenario: Live version is never fabricated

- **GIVEN** an Agent projection cannot resolve the running CLI version
- **WHEN** generator evidence is computed
- **THEN** the projection SHALL report unresolved evidence
- **AND** SHALL NOT present the pinned constant as the observed version

#### Scenario: Stale generated state is line-aware

- **GIVEN** on-disk artifacts record a generator older than the admitted line's expectations
- **WHEN** staleness is evaluated
- **THEN** the comparison SHALL use the admitted series' registry baseline
- **AND** artifacts generated by either admitted series SHALL NOT be flagged stale merely for differing from
  the other admitted series
