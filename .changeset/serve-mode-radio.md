---
'openspecui': minor
'@openspecui/core': minor
---

Replace the serve `[Y/n]` prompt with an interactive Direct Web / App Radio that remembers the chosen default in `~/.openspecui/settings.json`. Non-interactive sessions with no remembered preference now fall back to Direct Web and print a one-time hint explaining how to set a default with `--app` / `--web` or an interactive selection.
