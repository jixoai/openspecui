---
'@openspecui/core': patch
'@openspecui/server': patch
'@openspecui/web': patch
'openspecui': patch
'@openspecui/app': patch
'@openspecui/browser-translator': patch
---

OpenSpecUI 9.0.0 release preparation: this changeset records that the 9.0.0 major version bump is already applied on this release branch (`chore(release): apply changeset version 9.0.0`) and covers the remaining release-branch package changes (UI polish, CLI task-count projection, browser-bundle boundary fix) so the release gates see a complete changeset trail. It publishes as the first patch of the 9 line if consumed independently.
