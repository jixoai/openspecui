<!--
Orthogonal intents (updated 2026-07-31 Asia/Shanghai):
1. Bootstrap repository agents through CLAUDE.md.
2. Enforce protected-branch, PR, CI, and release delivery policy.
3. Protect static/SSG behavior and shared live/static projections.
4. Preserve the OpenSpec adaptation baseline, source-distinct projections, and independent-review corrections.
5. Fix App, project workspace, runtime-environment ownership, interaction-latency authority, CLI trace phase precision, buffered CLI process/Worker execution, live-projection loading, admission Pull, readonly refresh semantics, real-time visual lifecycle, OPSX-first information hierarchy, Config-owned Resolved Context, Terminal palette accessibility, container-responsive objective Kanban projection, live Board scroll ownership, OpenTray App-daemon hosting, App-build asset projection, Workspaces/Stores two-domain App information architecture, PWA retirement, App brand projection, foreground Server shutdown, and delivery-correction boundaries for the 1.6/6.1 line.

Original request (2026-07-14): "openspec 1.6.0 已经放出，我们需要开始进行适配，目前我们的进度有点落后。你先了解项目，然后更新 references/openspec，然后使用 $wayfinder 和我讨论具体的适配计划。我们最终使用openspec来管理 wayfinder 产出的文档。"
Original request (2026-07-28): "这个PR自身是否符合OPSX的开放式设计，是否会冲突？都需要进行深入的调查。"
Owner decision (2026-07-28): implement the objective Kanban rewrite and replace Dashboard Workflow Progress with ReadonlyKanban.
Owner layout correction (2026-07-28): "ReadonlyKanban必须是通过容器查询支持响应式。不允许发生横向滚动。空间足够的情况下就是 4*1，不是很够的情况下就是 2*2，拥挤的情况下就是 1*4。"
Owner live-layout correction (2026-07-28): "到kanban页面（/board）这里空间一旦不够，横向会出现双滚动条。另外纵向滚动也不合理，每一列应该要能独立滚动。"
Original request (2026-07-15): "我个人的想法，是把 --app 模式提上日程。因为 app 模式提供了多标签管理。它天生适合多项目管理的这种场景。"
Original request (2026-07-15): "我们可以在 cli 上新增一个 --auth 或者 --password。这样后端接口就必须带上这个 http header。"
Original request (2026-07-15): "我们这个项目本身只是 OpenSpec 的一个可视化投影，所以保持客观中立很重要。"
Original request (2026-07-15): "我们刻意开发了一个响应式内核，这是 openspecui 对 openspec 最大的增强。操作成功底层是要推送变更的，然后让多端基于订阅拉取更新。"
Original request (2026-07-15): "统计信息仍然有一定的间接价值。"
Original request (2026-07-15): "理论上是要的，你觉得呢，工作难度大不大？这是额外的工作还是可以和 live 版本保持尽可能的一致？"
Original request (2026-07-16): "接下来，你来接手后续工作"
Original request (2026-07-15): "你先负责后端（内核）的开发，我让ClaudeCode先帮你吧前端相关的代码先初步做一下，等一下你在它的基础上继续接手开发。"
Original request (2026-07-16): "/var/folders/tn/y_b12zxs2dldn8thmfnpy9c80000gp/T/openspecui-frontend-handoff.md 这是前端开发在发回交接文档。接下来，你来接手后续工作"
Original request (2026-07-16): "代码已经提交，开始review。如果有问题，那么可更新change甚至可以新开change。"
Original clarification (2026-07-17): "我会让worker开始具体的编码工作，你的责任是review+research+plan. 因此请你在GOAL.md 中明确规划具体的工作，我们的目的就是 apply change."
Original request (2026-07-17): "你要特别谨慎反复出现的问题，你应该在任务文件中强调相关的问题，给出具体的施工建议、反思建议。"
Original request (2026-07-19): "代码已经提交，开始review。如果有问题，那么可更新change。"
Owner direction (2026-07-19): "不要在 6.11 这个任务上徘徊了。你得把它拆开成多个独立的小问题，然后把阻塞的问题发给我，我来决策推进。"
Owner execution direction (2026-07-19): "我希望你加速交付，能并行工作的，就分发subagent一起做。"
Owner acceptance boundary (2026-07-20): "以后任何需要最终端到端的浏览器走查，就交给我来做。你最多负责到最基础的vitest+playwright的相关组件化的测试。"
Owner correction (2026-07-21): "每项先明确一个生产 owner、一个精准红例、一个绿例。focused review 未通过，不跑全量门禁。"
Owner clarification (2026-07-22): Launch Agent PTYs remain valid workflow Send targets; Planning PTYs require matching Root generation, and creating an Agent reveals Terminal when hidden.
Owner-reported defect (2026-07-22): "点击 Create 启动 Claude 后整个应用程序卡死，页面刷新也没有响应；Create 和 Send 都应该激活 Terminal 面板。"
Owner acceptance (2026-07-22): Single-page and multi-tab 6.14 walkthroughs passed; same-generation and changed-root Dialogs may remain open while showing `Refreshing planning root` when the resulting target/prompt converges correctly.
Owner-reported debt (2026-07-22): "整个过程中，几乎都在 Loading，切换个页面也等，做任何动作也在等，给我的感觉就是非常卡。"
Original request (2026-07-23): "现在页面数据的加载数据非常慢（比如dashboard页面、changes页面都要等待非常久，页面刷新后，似乎后台没有缓存一样，也要加载很久。"
Original request (2026-07-23): "走查任务直接到新的change中做。你目前的工作就是：review + interview + replan(write new openspec change)"
Original request (2026-07-23): "布局方面暂时不需要改动，后续社区有一个PR我会合并进来，那是关于kanban 的一个pr。所以本次change的主要优化点，在于更友好的UIUX，优化用户等待信息的时间感知。"
Owner UX direction (2026-07-23): "一次性把现有的页面都统一整改，因为这涉及到统一组件的封装和开发。全部改动，才能在中途暴露出所有隐含的可能、状态。这对于我们组件化的封装和开发非常重要。"
Owner UX direction (2026-07-23): "不用显示文字，可以用光影来替代，将它做成一种视觉语言，其实包括加载中等状态也是，尽量不要使用文字，而是使用视觉语言（动画、光影）等技术。"
Original request (2026-07-23): "请你深入调查，给出一份持有客观证据的调查报告，并给出‘系统性的解决方案’，并将它整理成 openspec change。"
Owner-reported defect (2026-07-26): An authenticated Project Web iframe retained `#credential=...` in its `src`, but root-route navigation removed the fragment before the document could consume it; subsequent `/api/health` and `/trpc/*` GET requests had no Authorization header and returned 401.
Owner execution request (2026-07-26): "行，那你现在把要修复的代码和测试先写完，完成后我再测试。"
Owner-reported defect (2026-07-26): "a内容还没加载出来，页面就自动刷新了。" Browser event evidence reduced this to a single iframe Document followed by root-to-Dashboard history canonicalization, not a second network navigation.
Owner-reported defect (2026-07-26): "Dashboard加载完成的一瞬间开始reload。" Extended browser evidence found a separate real reload at the 15-second App observation refresh: the iframe was detached and recreated with the same `src` and session because renderable health was cleared while the replacement probe was pending.
Owner-reported defect (2026-07-26): Opening B made A become `authentication-required`; opening C then did the same to B, and C failed after its later health refresh. The credential did not expire: each App window held only the locator credential it had directly consumed.
Owner architecture direction (2026-07-26): "深入阅读 OpenSpec 官方的源代码，了解某一项功能点依赖了哪些文件，使用哪些文件作为客观事实证据。将这些变更信息收集起来作为触发器，更新底层幂等计算的缓存结果，从轮询中彻底解放出来。"
Owner architecture clarification (2026-07-26): "最终计算结果本质是来自于 OpenSpec CLI 所提供的内容。即便现在有正在的任务，界面上仍然可以读到缓存，但它也能知道这个缓存现在正在被更新中。这是一套通用的数据拉取推送技术。"
Owner walkthrough acceptance (2026-07-27): "绝大部分功能基本都通过了，更多的问题是在一些 UI/UX 的问题上，这属于后续需要打磨的问题。"
Owner-reported defect (2026-07-27): Embedded Project Web Clipboard calls are blocked because the App iframe does not delegate the required Permissions Policy capability.
Owner-reported defect (2026-07-27): Missing/invalid Project Web credentials are objectively rejected, but the document remains Loading while health, tRPC, and PTY repeatedly report 401/UNAUTHORIZED.
Owner-reported defect (2026-07-27): SessionTabs Offline presentation can lag an iframe that already reflects disconnect; Inspector feedback is delayed and blur/focus appears to reconstruct its surface.
Owner-reported defect (2026-07-27): A clean SSG build emits a hashed Server entry while export assumes `dist-ssg/server/entry-server.js`.
Owner-reported defect (2026-07-27): Mobile Store Inspector overflows inline; AppHeader plus viewport-sized SessionTabs/iframe overflows the block axis.
Owner acceptance request (2026-07-28): "我需要非常具体的验收工具和验收流程"
Owner information-hierarchy correction (2026-07-29): "左侧导航栏的上方没必要放 Planning 信息，这和左侧导航栏的作用冲突了；点击等同于点击 Context。"
Owner information-hierarchy correction (2026-07-29): "Config > Project Binding 的 Store 使用 Combobox；其它卡片从产品价值重新组织，并用 Tooltip 提供解释。"
Owner information-hierarchy correction (2026-07-29): "Context 页面同理，从产品经理的角度重新组织有价值的信息。"
Owner layout correction (2026-07-29): "Settings 页面只能保留一个滚动 owner；ChangeDetail 的 Paths and CLI evidence 需要拆分可读摘要、artifact、References、CLI 结果，移动端不得横向溢出。"
Owner Terminal popup correction (2026-07-29): "弹出层的字体颜色要修复。"
Owner responsive direction (2026-07-29): "从产品经理的角度优化信息展示。以移动端的视觉体验优先，再逐步扩展到平板尺寸和桌面尺寸，使用容器查询做响应式。"
Owner same-root correction (2026-07-29): "Launch==Planning 的情况下，Dashboard 应隐藏冗余上下文，Terminal 不应提供 cwd 切换；ignored Store warning 仍应在 Config 中可修复。"
Owner Context placement (2026-07-29): "把它作为 Config 页面 title 部分的一个 Actions，点击进入一个 config/context 的子页面，这个页面顶部提供一个返回按钮。"
Owner Context readability (2026-07-29): "将结构化信息的可读性增强，并利用手风琴折叠隐藏次要信息，用 tooltip 隐藏冗长信息，用 tooltip 提供帮助信息。"
Owner accessibility correction (2026-07-29): "终端的字体色相关的使用必须统一，因为在 light 环境下终端背景可能仍然是黑色或者灰色。"
Owner presentation direction (2026-07-28): "backend a 会重新打开一个浏览器窗口，而不是聚焦原本的窗口；从底层封装，后续可能对接 OpenTray 原生窗口。"
Owner acceptance feedback (2026-07-28): "基本全部通过。列表骨架之间需要 gap 或分割线；Static 导出后的 /context 页面没数据。"
Owner-reported defect (2026-07-28): "pnpm openspecui export -o ./tmp --open 导出的数据好像逃逸到 html 去了。"
Original request (2026-07-28): "把残留的工作先完成"
Original request (2026-07-28): "我想先发布一个beta版本"
Owner stable release authorization (2026-07-29): "开始发布"
Original request (2026-07-29): "立项 6.1.x: 我们要继续打磨 app 模式，我们需要将它适配对接 opentray。支持 --web；对 overlay-window-controls 的样式适配；使用 appMode。"
Owner daemon direction (2026-07-29): "openspecui [project] 是 openspecui serve [project] 的缩写；start/stop/restart 只管理 App daemon。serve 自动发现 daemon，交互式缺失时询问 [Y/n]，非交互默认 Direct Web。"
Owner command direction (2026-07-29): "openspecui --app 等价于 openspecui serve --app；openspecui --web 等价于 openspecui serve --web；App 中 Workspaces 替代 Sessions，并提供 Open in browser。"
Owner distribution correction (2026-07-29): "放弃 app-url 支持；daemon 使用随 CLI 发布的本地 App 外壳，不再考虑 App 外壳部署位置。"
Original request (2026-07-30): "我个人觉得，左侧只留下 Workspaces + Stores 就行了，你觉得呢？"
Owner App information-architecture direction (2026-07-30): "Workspaces融合了Connections，点击`+`，那么弹出的Dialog就会包含Connnections列表，而不是一个URL-Input"
Owner Stores direction (2026-07-30): "Stores 完全可以融入 `Environment Center` 这个东西，就跟 Config 和 Context 的关系一样。"
Original request (2026-07-30): "Workspace需要记住曾经打开的目录，并且支持收藏。关键是，支持直接从目录直接启动 openspecui 服务。"
Owner managed lifecycle decision (2026-07-30): closing a Workspace tab preserves its managed backend; explicit Stop terminates it; daemon stop affects only daemon-managed services; daemon restart restores the managed running set.
Original request (2026-07-30): "所有正在运行中的backend都会显示在这里。"
Original request (2026-07-31): "Workspace Home 页面不要有PWA安装，我们现在已经完全废弃pwa这个方向了。请清理干净pwa相关的代码"
Original request (2026-07-31): "左侧导航栏顶部这里的 OpenSpecUI App，这里的icon改成我们的 logo"
Original request (2026-07-30): "任务管理器...可以杀掉Workspace，或者收藏、取消收藏"
Owner correction (2026-07-31): "TaskManagerPage 改成 TaskManagerDialog"; external ownership must not leak the internal `ext` abbreviation, and ephemeral loopback App origins must reach healthy local backends.
Original request (2026-07-30): "弱化端口这个概念，重点强调 path的概念。"
Original request (2026-07-30): "Tab这里默认写仓库路径 org/repo，如果没有就使用path的foldername；subtitle写git分支名"
Original request (2026-07-31): "系统性地进行修复，因为List页面也有类似的问题。所有可能其它页面都有类似的问题。"
Owner-reported defect (2026-07-31): "终端大量报错，比如: Cannot execute the operation on ended Span"
Original request (2026-07-31): "dashboard.refreshGitSnapshot?batch=1 这个请求一直在阻塞其它任务，这个不是只读吗"
Owner correction (2026-07-31): "这个只是辅助‘只读’的动作，它的本质仍然是Readonly，所以把它改成只读才是解决问题的根本"
Original request (2026-07-31): "这些命令的执行，时间绝对不是七八秒那么久...请看一下代码，看能不能让trace更精确"
Original request (2026-07-31): "直接寻址到本地 openspec背后的js，直接用 worker_thread 来运行它。"
Original request (2026-07-31): "在主线程，通过 OPENSPEC_SPAWN_MODE=process|worker 来进行区分两种模式。"
Owner acceptance (2026-07-31): Worker execution is materially faster and becomes the default; process is explicit or a fallback only when the importable OpenSpec CLI JavaScript module cannot be found.
Owner finding (2026-07-31): `VP_NO_UPDATE_CHECK` is not a contributing factor to the observed CLI startup latency.
Owner diagnosis (2026-07-31): same-generation readonly cache misses single-flight before lock admission; slow Root CLI resolution and record retirement stay outside short generation-checked write commits.
Owner Dashboard hierarchy (2026-07-31): Historical Trends remains unchanged; Kanban owns a full row; Active Changes and Code Git Snapshot share the following responsive row; the Specifications list is removed while its Summary metric remains.
Original request (2026-07-31): "让这个页面的布局更加稳定，基于真实的布局去做骨架屏，或者说是直接让卡片自身去支持 Pending 样式"
Owner Dashboard stability direction (2026-07-31): Historical Trends keeps fixed card heights; compact Kanban keeps a container-responsive fixed viewport whose four groups own independent vertical scrolling; Dashboard Git shows at most five current-worktree activity rows, includes changed Uncommitted plus four commits, hides empty Uncommitted, and omits detached Other Worktrees from the default surface.
Original request (2026-07-31): "检查目前的这个 Code Git Snapshot，它非常慢，有时候甚至要十几秒，请你做实验，检查它的工作到底做了什么，为什么需要那么多的时间"
Owner Git refresh timing direction (2026-07-31): hidden documents pause the timer while preserving an absolute next-refresh deadline; visibility resumes the remaining delay or requests one coalesced refresh when overdue.
Owner App-build correction (2026-07-30): "不是 pnpm dev 的责任，而是 app 项目自身的构建本身就要有这个 copy 行为；直接对标 prod 版本。"
Owner native-chrome correction (2026-07-30): "我要的自绘 window-titlebar；pnpm openspecui 开发模式应启动 OpenTray DevTools。"
Owner-reported defect (2026-07-31): Tray Quit left a half-closed daemon App HTTP listener alive; an old daemon then served the new App shell's directory API request as HTML and exposed a raw JSON parse failure plus duplicate offline states.
Owner navigation/chrome correction (2026-07-31): Workspace secondary rows never highlight; a right-side signal alone reports verified Running, while OpenTray hides duplicate sidebar branding and names its titlebar `OpenSpecUI App`.
Owner sidebar correction (2026-07-31): clicking either `[icon] OpenSpecUI App` toggles one shared desktop sidebar state; the collapsed inline brand keeps only the logo and the rail renders primary navigation icons only.
Owner sidebar-motion correction (2026-07-31): expansion/collapse is one native same-document View Transition; the live sidebar performs no transform, width, padding, or timer animation. Sidebar old/new snapshots share viewport-minus-titlebar height, a fixed expanded-width stage, and left/top alignment: wide-to-narrow pairs old right-first `0→100` with new left-first `70→0`; narrow-to-wide pairs old left-first `100→0` with new left-first `70→0`, so both views hand off on the same left edge. Shell old/new snapshots share that height, a fixed viewport-minus-collapsed-sidebar stage, and right/top alignment: narrow-to-wide pairs old right-first `0→100` with new right-first `30→100`; wide-to-narrow pairs old left-first `100→0` with new right-first `30→100`. All paired masks share one easing and overlap without a blank handoff frame. Shell snapshots additionally use a subtle right-anchored scale-x handoff; the VT groups and live DOM remain free of geometry transform animation.
Original request (2026-07-28): "让界面像5.x那样清爽的同时，仍然保证6.x新增的信息可以被Get到。因为我们openspecui最关键的是OPSX这套流程，其它都是服务于这套流程的。"
Review correction (2026-07-20): Terminal cwd evidence must cross the production Server owner instead of injecting a hand-authored downstream callback.
Review correction (2026-07-20): Settings tool delivery must preserve upstream physical artifact scope, render-time subscription provenance, and CLI-runner cache retirement.
Product information architecture law (2026-07-29): Project navigation must not render a clickable Planning summary that duplicates `/context`; the shell may expose only route navigation, while Context owns root identity and explanation. Project Binding owns launch `store:`/`references:` editing and should present registry Store suggestions through a freeform Combobox without making registry lookup a save prerequisite. Context answers root, launch project, Store/References, and action readiness first; timestamps, raw command envelopes, data-scope paths, and diagnostics remain deeper evidence. Settings has one page-level scroll owner. Change evidence is layered as human summary -> readable paths/artifacts/references -> raw CLI payload/stdout/stderr, with no path or JSON block forcing horizontal overflow.
-->

MUST READ: CLAUDE.md

## Branch Protection Policy

- `main` is protected. Do not push commits directly to `main`.
- All code changes must be submitted via Pull Request from a feature branch.
- Merge to `main` only after required CI checks pass.
- For PRs that change publishable packages, include a `.changeset/*.md` file.  
  Exception: docs-only or CI-only changes that do not affect package behavior.

## Delivery Workflow

- Always run CI-equivalent local checks before opening/updating a PR.
- Required local checks (match CI gates): `pnpm format:check`, `pnpm lint:ci`, `pnpm typecheck`, `pnpm test:ci`, `pnpm test:browser:ci` (or a clearly scoped subset when changes are package-local and justified in PR notes).
- Every changed TypeScript/TSX physical file, including tests, must retain an accurate timestamped orthogonal-intent/original-request header; a production-only header audit is incomplete.
- Review handoff law: the reviewer owns research, independent review, OpenSpec artifact corrections, and an executable `GOAL.md`; the assigned worker owns `openspec-apply-change`, implementation, tests, checkpoint updates, commits, and PR delivery. A worker Goal must request applied code and evidence, not another plan-only or review-only pass.
- Browser acceptance ownership law (2026-07-20): the owner performs every final end-to-end browser walkthrough and manual acceptance. Agents may build and run only focused Vitest coverage and basic component-level Playwright fixtures. Automated component/fixture results are preparation evidence, never a substitute for the owner's final walkthrough; worker Goals must stop at that boundary and report the exact automated evidence.
- Owner acceptance tooling law (2026-07-28): final walkthrough handoff must use numbered, production-boundary-specific cases with exact setup/trigger/restore commands and explicit PASS/FAIL observations. Disposable fixtures and automated checks prepare evidence only; result ledgers must record the exact head and exclude credentials, Authorization headers, and private launch fragments.
- Counterexample evidence law: a test described as red evidence must be shown to fail at its named fixed point for the intended reason. Manually invoking an already-correct downstream handler, or rejecting a different lifecycle phase, is characterization evidence rather than proof that the reported production defect was captured. Record that distinction honestly in the Change.
- Mutation-resistance evidence law: when acceptance depends on hidden lifecycle bookkeeping, prove that the test fails after removing or bypassing the exact cleanup transition. A terminal-event assertion alone is insufficient when an earlier `settled` guard can mask leaked child ownership or listeners.
- Typed test-evidence law: a passing transpile-only test is not type-safe evidence when its file is excluded from package typecheck. Tests that prove public Router, Manager-owned service, Adapter, or typed CLI boundaries must use checked fixtures without `as never`, `as any`, non-null assertions over fabricated state, or suppression comments; place reusable fixtures in a typechecked source or add an explicit test-typecheck lane. Never weaken the production contract to make a fixture compile.

## Static/SSG Guardrails

- Never trust old `dist-ssg` outputs when validating static UI changes.
- For any static/export/config/dashboard change, always rebuild SSG before judging results:
  - `pnpm --filter @openspecui/web build:ssg`
- If behavior looks unchanged, clean stale artifacts first, then rebuild:
  - `rm -rf packages/web/dist-ssg packages/web/.vite`
  - `pnpm --filter @openspecui/web build:ssg`
- Browser-target code in `packages/web` must not runtime-import `@openspecui/core` root entry.
  - Allowed pattern: `import type { ... } from '@openspecui/core'`
  - Runtime helpers must use safe subpath entries (example: `@openspecui/core/opsx-display-path`).
- If static snapshot structure changes, update these together in one PR:
  - `packages/core/src/export-types.ts`
  - `packages/cli/src/export.ts`
  - `packages/web/src/lib/static-data-provider.ts`
  - related tests in `packages/web/src/lib/static-data-provider*.test.ts`
- Static and live modes must share the same display/mapping logic; do not introduce parallel implementations unless explicitly required.
- Static Context publication law (2026-07-28): `/context` remains part of both live and static information architecture. Static Context is selected only from the publication-redacted `ExportSnapshot.meta`: project label, observation/version facts, display-safe Planning-root provenance, writable Store id, and the explicit Reference export policy. It must never fabricate `RootContext`, start a live subscription, or expose CLI availability/raw evidence, registry/data-home/`envUri`, diagnostics, or mutation authority. `omit` publishes only its aggregate Reference count; `include` may publish only the Store summaries already present in the snapshot. The SSG route manifest must generate `/context`, not merely register a client route.
- Static document embedding law (2026-07-28): every dynamic value inserted into generated HTML must cross `packages/web/src/ssg/static-document.ts`. Never interpolate raw `JSON.stringify` output, base paths, route titles, or snapshot/file-preview content directly into a script or HTML text context. Inline-script JSON must preserve value round trips while escaping HTML-significant `<` plus JavaScript line separators; title text must be HTML-escaped. Regression evidence must parse a real generated document, prove `window.__INITIAL_DATA__` is available, and prove source text did not escape into visible DOM or executable markup.
- Static-related completion check (minimum):
  - `pnpm --filter @openspecui/web test -- src/entry-client-static.test.tsx src/lib/static-data-provider.opsx.test.ts`
  - `pnpm --filter @openspecui/web build:ssg`
- A successful SSG build is not export evidence. The clean Server build and the exporter must share one deterministic,
  machine-readable entry contract; run the real CLI export path after the clean build. Never assume an unhashed
  `dist-ssg/server/entry-server.js`, scan for an arbitrary JavaScript file, or fall back to stale output.

### Community Contributor Mode

- Do not open/update a PR until local CI-relevant checks pass.
- After PR is open, wait for maintainer review and merge decision.

### Manager Mode (Gaubee)

- Do not open/update a PR until local CI-relevant checks pass.
- After required PR checks pass, auto-merge to `main`.
- After merge to `main`, ask manager whether to release.
- If manager confirms release:
  1. run `pnpm changeversion` for stable or `pnpm changeversion --pre <channel>` for an explicitly requested prerelease
  2. wait for the changeversion PR checks to pass
  3. auto-merge the changeversion PR
  4. wait for GitHub Actions `release.yml` on `main` to publish packages and push tags
  5. notify manager only after the GitHub release automation succeeds

- Prerelease delivery law (2026-07-28): A requested beta release enters or continues explicit Changesets prerelease mode and derives npm dist-tag plus GitHub prerelease state from the generated SemVer version; caller-authored parallel channel flags are forbidden. `6.0.0-beta.0` publishes under `beta` and cannot move `latest`. Semantic flag conflicts belong to the typed prerelease planner because a CLI parser may materialize absent boolean flags as `false`; direct production-parser evidence is required. Registry publication and package-tag delivery are independent facts: a rerun must recover missing tags after packages already exist, while a true registry-and-tag no-op must not rewrite an old GitHub Release. Release automation pushes tag refs only, never its potentially stale checked-out `main`. Completion requires the exact-head workflow, npm channel state, remote tags, and GitHub prerelease as separate evidence.
- Stable promotion law (2026-07-29): when `.changeset/pre.json` still records an active prerelease channel, a stable release MUST explicitly exit that channel through `pnpm changeversion --exit-pre`; plain `pnpm changeversion` is valid only when no prerelease state exists. The version PR, merged `main` head, npm `latest`, remote package tags, and non-prerelease GitHub Release remain independent completion facts.

## OpenSpec 1.6 Adaptation Baseline

### OPSX-first information hierarchy law (2026-07-28)

OPSX decisions own the direct visual plane: current task, next action, mutation state, authority loss, errors, and
blockers remain visible without hover or expansion. Secondary scan facts such as Root source, Store, References,
schema, freshness, and counts use compact, keyboard-reachable `Badge + Tooltip` presentation. Verbose paths, raw
CLI envelopes, provenance, successful settlement history, and low-frequency diagnostics belong to a collapsed
Accordion or equivalent evidence disclosure. A failure is always promoted to the direct plane; Tooltip-only error
or stale-authority presentation is forbidden. Shared information components are pure presentation owners and must
not acquire subscriptions, infer aggregate health, authorize operations, or fork static/live semantics. Settings
summarizes and links to the owning Context/Config surfaces instead of duplicating them; Context retains complete
objective evidence but keeps raw command envelopes collapsed by default.

### Dashboard stable Pending geometry law (2026-07-31)

Dashboard keeps the settled information topology mounted during admission; a page-level skeleton may not replace
Historical Trends, Kanban, Active Changes, or Code Git Snapshot. Each region owns Pending inside its real card or
panel geometry. Historical Trends cards use fixed responsive heights. Compact Kanban uses a fixed
container-responsive viewport with `1x4`, `2x2`, and `4x1` row topology, and every group owns its vertical scroll;
the page never becomes the lane scroll owner. Current-worktree Git activity publishes at most five rows: changed
Uncommitted plus four newest commits, otherwise five newest commits; an empty Uncommitted row and detached Other
Worktrees stay outside the default Dashboard surface. Active Changes and Code Git Snapshot may keep dynamic settled
heights because they occupy the final page row. The Server computes full activity only for the current worktree;
available non-detached Other Worktrees receive summary-only ahead/behind/diff data, while detached or unavailable
worktrees execute no hidden detail commands. Git subprocesses inherit Projection Work cancellation so a retired
generation cannot retain the single Git resource slot.

### Terminal palette boundary law (2026-07-29)

Terminal-owned chrome and popups that are physically mounted inside `terminal-surface` derive neutral surface,
foreground, border, and hover tokens from the active xterm palette. Their legibility must not depend on the
application light/dark theme. Application-owned dialogs and Settings remain outside that scope and retain the
application palette. Never enforce a descendant-wide Terminal color: explicit error, success, activity, focus,
selection, and notification colors remain semantic overrides.

### Container-responsive information surface law (2026-07-29)

Project information surfaces default to one readable mobile column and add density only when their nearest
inline-size container can sustain it. Config forms, Context summaries, Settings field groups, and Change evidence
must use container queries for internal topology; viewport breakpoints are forbidden because the same viewport may
contain a mobile-width project surface beside the desktop rail. The shell may still use viewport/media queries to
choose its navigation topology. Narrow, intermediate, and spacious evidence must be verified at the rendered
container boundary, not inferred from viewport width alone.

### Config-owned Resolved Context law (2026-07-29)

Config owns declarations; Resolved Context owns the objective result after OpenSpec CLI root selection. Context is
therefore a Config title action and the canonical route is `/config/context`, with a direct return to `/config`;
it is not a persistent project navigation tab. Live and static route trees, SSG publication, notifications, and
all internal links use that same route. The direct plane answers effective Root identity and current action
authority first. Structured Launch/Planning relationships and Reference summaries follow. Long diagnostics,
Context members, data scope, and Doctor/Context command envelopes use separate collapsed disclosures. Tooltip is
limited to short terminology help, compact status explanations, and complete single values; errors, blockers, and
multi-record evidence must never exist only inside Tooltip content.

### Root-topology presentation law (2026-07-29)

Root topology and configuration hygiene are independent facts. A shared, subscription-free presentation selector
classifies current server-observed physical identities as `collapsed`, `distinct`, or `unresolved`; Root source,
Store id, warning text, Git scope, and lexical display-path equality are not topology substitutes. A missing
server-observed physical Launch identity remains `unresolved`; it must never fall back to the display/cwd path. In
the healthy collapsed default, Dashboard
omits its redundant context band and generic Terminal creation implicitly remains `launch-project` without a cwd
selector. Workflow-locked Terminal creation remains `planning-root`, preserves expected Root generation, and does
not weaken the public PTY protocol or stale-generation guard. Project Binding owns repair of an ignored `store:`
declaration: `root_pointer_ignored` is a non-blocking warning with a clear action through the existing draft/save
flow. Context combines Launch and Planning into one `Project root` summary only when topology is collapsed; source,
provenance, warnings, failures, and raw evidence remain retrievable, and exceptional states stay direct.

### Terminal palette accessibility law (2026-07-29)

Terminal chrome is a palette-local surface independent from the application light/dark theme. The Terminal panel
owns neutral foreground, secondary foreground, elevated neutral surface, and border tokens derived from the active
xterm palette; all built-in neutral foreground/background pairs must meet WCAG AA contrast. Do not enforce one
color through a universal descendant selector because explicit error, success, activity, focus, selection, and
notification colors must remain semantic. Command dialogs, Terminal Settings, and other application-owned overlays
remain outside this palette scope.

### Objective Kanban projection law (2026-07-28)

Kanban is a visual projection over OpenSpec facts, never a persisted workflow. Its four lanes are exactly active
`TrackedTaskPhase` (`no-tasks`, `in-progress`, `complete`) plus structural `archive`; do not rename them TODO, QA,
Done, verified, synced, valid, or archive-ready. Dashboard and static publication consume a shared
`ReadonlyKanban` with navigation but no operation callbacks. Live Board operations are explicit capabilities:
Apply opens the existing Compose Operator and Archive opens the existing Archive Operator; drag-to-archive is only
another launcher for that Operator and never mutates lane/task state. Board and Change Detail must consume the same
Operator hook, and live commands remain disabled whenever Root or the relevant projection is not current. Changes
and Archives settle as independent realtime regions; preserve progressive rows, row errors, retained revalidation,
and regional failures rather than restoring a combined full-page loading gate. `ReadonlyKanban` owns an inline-size
container-query boundary: it renders four columns in one row when spacious, two columns in two rows when constrained,
and one column in four rows when crowded. Viewport breakpoints and horizontal scrolling are forbidden for this
readonly projection; compact/full variants may change content density but never this responsive topology.
The live Board follows a separate scroll contract: its route consumes the shell's remaining block-size and contains
page overflow; the lane grid is the only inline-axis scroll owner; every lane header stays fixed inside its lane and
every lane body owns an independent block-axis scrollbar. Do not let the route, `main-content`, a lane section, or
`RealtimeRevalidateCue` become a competing scroll owner.

### 6.11 execution-scope law (2026-07-19)

Checkpoint 6.11 is now limited to W1 Git scope/token delivery plus W4 single-page direct-Web evidence
and multi-tab unit coverage. The owner will perform the manual multi-tab acceptance. W2 Project Binding
settlement and W3 WebSocket/reactive error propagation are explicitly deferred to independent follow-up
Changes; they must not be implemented incidentally while closing 6.11. Candidate browser observations
remain characterization/fixture evidence until a checked public-boundary red and mutation-resistance
proof exist.

- W2's agreed future contract is write-then-converge: the launch binding write returns typed
  `rootPreview`/transition evidence, and subscriptions converge the Planning root asynchronously.
- W3's agreed future behavior is to expose the real transport/API error rather than inventing a UI lock or
  stale-data success state. The current split between HTTP queries/mutations and WebSocket subscriptions
  must be tested and changed only in that follow-up contract.
- Follow-up Change directories are `target-openspec-cli-16-project-binding-settlement` (W2) and
  `target-openspec-cli-16-reactive-error-propagation` (W3). Their Goals are independent worker
  authorizations and must not be folded back into checkpoint 6.11.
- 6.11 acceptance uses direct same-origin Project Web with the pinned CLI and isolated `XDG_DATA_HOME`;
  the experimental App iframe is excluded.

- `references/openspec` is the official upstream submodule and currently targets exact tag `v1.6.0` (`e1b51d1`).
- The shipped line is OpenSpecUI 5.x nominally targeting OpenSpec CLI 1.5.x. Version compatibility alone is not proof of feature completeness: the 1.4 `sync` core-profile change and the 1.5 resolved-root/Stores contract must be audited as part of the 1.6 plan. The established version law is one OpenSpecUI major per OpenSpec CLI minor; the 1.6 adaptation therefore plans the next major line unless an explicit decision changes that law.
- OpenSpec CLI remains the workflow source of truth. Prefer CLI JSON and exit status over parallel parsers or inferred workflow state.
- OpenSpec 1.6 Doctor References carry Store identity, root, and diagnostics, but no embedded Spec index. Build Referenced Spec Catalog entries by enumerating each direct Doctor-declared Reference through typed `list --specs --store --json`; never treat an absent `references[*].specs` field as an empty Store.
- Typed CLI contract law (2026-07-16 independent review): `.passthrough()` tolerates additive unknown fields but does not make known OpenSpec 1.6 fields strongly typed. Command consumers must use the typed result envelope and preserve parsed data, raw payload/stdout, stderr, diagnostics, contract drift, success, and exit status as separate facts; never reparse `stdout` after a typed executor call. Preserve explicitly supplied Store selectors by presence rather than truthiness, keep the beta Store UI projection optional-field tolerant, and require executable pinned-CLI fixtures before claiming feature-complete adaptation.
- OpenSpec 1.6 adds the `update` planning workflow, Oh My Pi support, Trae command delivery, stricter archive/validate behavior, and tracked-task glob resolution. Audit profile workflows, tool initialization state, public workflow hooks, UI actions, validation/archive contracts, and task projection together.
- OpenSpec 1.6 tracked-artifact progress and OpenSpecUI's schema-wide checklist aggregate are different facts. The planning decision is recorded: only `trackedTaskProgress` drives workflow state; `documentChecklistSummary` remains secondary analytics, and `applyInstructionProgress` preserves the raw Apply result.
- The adaptation is tracked by `openspec/changes/target-openspec-cli-16-line/`. Wayfinder research, maps, and tickets live under that change's `wayfinder/` directory, then converge one-way into formal OpenSpec artifacts under `loop/` when implementation scope is settled.
- The product model has three layers: root-correct operation on the single CLI-selected planning root; context-aware presentation of read-only References; and higher-level Store/Context/Workset orchestration. Root correctness and Reference-aware project surfaces form the 6.0 correctness baseline. App-native Store administration remains experimental and is not a 6.0 release gate; Workset orchestration remains out of scope. Do not describe Stores as a package-monorepo graph.
- OpenSpec 1.6 `references:` resolves registered Store identities, not raw paths. `store setup` targets standalone planning repositories and rejects paths nested inside an existing Git repository, but `store register` can promote an existing healthy root inside a monorepo even when that root has no `.git` of its own. A Store is therefore not strictly one-to-one with a Git boundary. `store:` remains a fallback for a config-only root rather than a link between two writable roots.
- Store registration is user/machine-local and shared across projects by default at the XDG/global data location; OpenSpec has no project-local registry and `references:` never auto-registers. `XDG_DATA_HOME` can isolate a process environment's registry, but that is an environment boundary rather than a committed project declaration. Use the CLI registration path as the supported mutation surface because it validates identity/root health, canonicalizes paths, detects conflicts, and locks registry writes.
- `XDG_DATA_HOME` selects the whole OpenSpec user-data root: Store registry, Worksets, and user schema overrides. It is a single replacement scope, not a global-plus-project overlay. Do not expose it as `StoreRoot` or silently place it at the project root, where `${XDG_DATA_HOME}/openspec/schemas` collides semantically with project schemas.
- OpenSpec environment-global config is a separate XDG config fact: `${XDG_CONFIG_HOME}/openspec/config.json` when set, otherwise the pinned CLI's platform fallback. `openspec config list --json` reads that global config only; it does not inspect project config or discover project/profile drift. Never treat `XDG_DATA_HOME`, Store-registry invalidation, Planning-root Context, or project schema changes as Environment Global config dependencies. Resolve the path through `openspec config path` and observe that exact file reactively.
- Any project-scoped OpenSpec data design must keep CliExecutor, workflow invocations, embedded terminals, and Agent-executed OpenSpec commands on the same effective scope. Do not load unrestricted repository-owned environment variables into PTYs or general subprocesses; a project `.env` is a trust boundary, and generic XDG injection affects non-OpenSpec programs too.
- Decision (2026-07-15): OpenSpecUI inherits the launch environment's `XDG_DATA_HOME` and preserves it across every OpenSpec execution surface. Do not add `openspec/.env`, generic `config env`, `StoreRoot`, project registry synthesis, or registry overlay behavior in the 1.6 adaptation. Lock existing CLIExecutor/PTY inheritance with tests; treat any future data-scope manager as a separate product change.
- Implementation law (2026-07-16): Core `RootContext` is the single project-workspace projection of CLI availability plus `doctor --json` and `context --json`. It preserves the exact upstream root source (`nearest | declared | store | implicit`), direct Reference diagnostics, raw stdout/stderr/exit evidence, and inherited data-scope provenance. Server query/subscription and Web Context must consume this contract; do not restore local placeholder root-source names or a second frontend model. `PlanningRootServiceManager` is the sole owner of root-scoped Adapter, Document, OPSX Kernel, Search, Dashboard, and file-preview services; only explicitly launch-scoped operations may use `launchProjectAdapter`.
- Workflow action law (2026-07-16): `PlanningRootServiceManager` also owns `WorkflowInvocationService` and its planning-root HookRuntime. Every change action returns one explicit workflow target plus command-specific `workflow-status | artifact-instructions | apply-instructions` evidence. Preserve parsed data, raw JSON stdout, stderr, exit status, structured diagnostics, `changeRoot`, planning home, artifact paths, concrete glob outputs, action context, References, and the exact explicit-Store selector. Agent prompts wrap raw CLI evidence with the resolved planning root and explicitly forbid reconstructing `<launch-project>/openspec`; OPSX and direct CLI commands retain the same Store selector on every supported follow-up. Workflow hooks receive this target instead of an ambiguous `projectDir`; this breaking contract is `OnRunWorkflowHookV2` with protocol version 2 and no v1 alias. Static fallback results explicitly carry no backend target/evidence.
- Root action readiness law (2026-07-16): Root-dependent actions unlock only for a current `ready` Root Context. Initial loading, refresh with stale data, transport failure, and CLI-owned error states remain locked. Error UI preserves the failed attempt's root, Store, Doctor/Context exit status, stderr, contract drift, and structured diagnostics. OpenSpec Reference warnings remain visible evidence but do not turn a successfully resolved writable root into failure; only error-level Reference diagnostics may block it. Buffered and streaming validate/archive procedures derive explicit Store selection server-side from Root Context; clients cannot choose an independent Store.
- Root Context notification law (2026-07-22): `NotificationService` records remain local to the Server instance that owns the `PlanningRootServiceManager`; App may aggregate presentation state later but never merges records. The Server-owned bridge baselines its first terminal state and emits only root failure, recovery, or resolved Planning-root identity change. That identity is root path/source plus effective Store id, never generation or data-scope diagnostics. `loading`, `refreshing`, same-root generation renewal, Reference warnings, raw CLI evidence, paths, Store inventory, `envUri`, and data-home values are not notification payloads. Late callbacks must cross both the active subscription epoch and retired Manager generation boundary before publishing; observable `unsubscribe()` alone is insufficient.
- Config ownership law (2026-07-16): The public `planningConfig` boundary has three non-interchangeable facets. `project-binding` reads and edits only the launch project's `store:` and `references:` declarations and carries a Root Context preview even when selection fails. `active-root` reads/writes the CLI-selected planning root's schema/context/rules config and reports when that root is external to the launch project. An existing empty active-root config is still an existing editable file; file presence and content truthiness are separate facts. `environment-global` projects `openspec config path` plus `config list --json` and the inherited data scope. Do not restore ambiguous `opsx.projectConfig` or `cli.get/setGlobalConfig` endpoints, and do not infer that environment-global state is project-owned.
- Config surface ownership law (2026-07-17): `routes/config.tsx` composes tabs and owns only the Schema workspace. `ProjectBindingSection`, `ActiveRootConfigSection`, and `EnvironmentGlobalConfigSection` each own their subscription, draft, mutation, pending/error lifecycle, and direct tests. Project Binding preserves dirty declarations on failure; Active Root branches on `exists` and keeps an existing empty file editable; Environment Global preserves unknown JSON fields, raw CLI evidence, and the dedicated typed Planning-root Update transport. Static mode renders only the exported Active Root snapshot and states that Launch binding and environment-global state are unavailable.
- Project Binding settlement correction law (2026-07-19): A launch binding may report `write-complete` only after the shared physical/reactive writer has rejected symlink escape and settled file/directory/existence/stat projections. The public update result is a correlated union: `ready` preview pairs only with `converging`, while an error preview pairs only with the same `preview-error` evidence. Stale A, preview failure, or subscription error keeps the launch-owned repair controls available; editing after a save retires that save's pending draft generation so a late B emission cannot clear or overwrite the newer draft. Public RPC comments and checked tests must prove these pairs and the exact retirement transition; disabling controls is not repair-path evidence.
- Reactive transport closure law (2026-07-28): Queries and mutations use typed HTTP, lifecycle Push subscriptions use tRPC WebSocket, and PTY uses its dedicated WebSocket protocol. Retained data may remain displayable through reconnect or failure but never restores mutation authority; terminal transport and API errors remain visible instead of being rewritten as Loading or stale success. The W3 follow-up was reconciled from later accepted production work and archived without new production code or fabricated historical red evidence.
- Config readiness/reactivity law (2026-07-18): Every root-dependent mutation reachable from Config, including retained Schema/Template writes, Active Root writes, and typed Update (including Apply auto-Update), consumes the shared `useRootActionState`; stale snapshots remain display-only. Environment Global profile/drift and JSON/raw evidence must come from one reactive CLI-owned projection or equivalent subscription, never a second `cli.getProfileState` query over the same config. Project Binding shows direct Doctor Reference evidence and Active Root states the objective shared-Store consequence without inferring health, completeness, or affected-project inventory.
- Config correction evidence law (2026-07-18): A Config loading lock must cover the actual asynchronous refresh/rebind window, not only a synchronous request turn. Stale Environment Global projections cannot write through editor, shortcut, or already-open dialog paths; Active Root retains a local Cancel path when readiness changes. Effective OpenSpec profile defaults, including omitted core `workflows`, must be projected from pinned CLI semantics, and red evidence must reach the real mutation function rather than only a disabled control or mocked downstream call.
- Config correction timing law (2026-07-18): A refresh completion signal is authoritative only after the replacement projection is committed to the consuming component; resolving inside the transport callback can make an awaited second operation observe stale refs and skip or misclassify work. OpenSpec `core` profile semantics always return the pinned Core workflow set regardless of raw `workflows` presence/content; only `custom` honors the explicit list.
- Settings OpenSpec diagnostics law (2026-07-20): Settings composes read-only OpenSpec runtime diagnostics from the shared Root Context, the existing Environment Global subscription, and reactive tool-delivery state. CLI availability/version comes only from Root Context; the fixed global installer must retire the `ConfigManager` CLI-runner cache, make an in-flight old resolution unable to repopulate it, and then invalidate Context before terminal or indeterminate settlement instead of retaining a second `cli.checkAvailability` display truth. Settings must not restore or retain `cli.getProfileState`, duplicate Config's profile/Update mutations, read generated project-local artifacts from the Planning root, or infer cross-subscription ordering. Project tool detection, skills, and ordinary commands are Launch-owned, but official Codex commands are environment-global under `${CODEX_HOME:-~/.codex}/prompts`; observation roots and UI labels must preserve that upstream physical scope rather than calling every artifact Launch-owned. A delivery/workflow A -> B rebind must mask A during B's first render/commit and retire late A callbacks; a passive-effect cleanup alone is not provenance. Root/CLI evidence links to Context, environment-global evidence and edits link to Config, and static Settings remains Appearance-only. Tool detection and initialization state are filesystem projections and therefore require subscriptions rather than manual query/refetch loops. Keep Root/Environment diagnostics, tool initialization, and their thin Settings composition in separate physical modules rather than replacing one oversized route with one oversized component.
- Mutation-boundary evidence law (2026-07-18): Root readiness tests must rerender a new blocked state and invoke the real mutation owner; in-place mutation of an old mock object or disabled-button assertions can hide stale closures. Schema/Template file writes and Active Root Save require current-gate checks before transport.
- Dialog ownership law (2026-07-18): Environment Global Profile Apply is runtime-environment scoped and remains writable when its projection is current; only direct planning-root Update and Apply's optional second Update use Root Action readiness. A Root loss must not turn a valid global-config write into a blocked operation; it must skip/report only the root-dependent second operation.
- Git repository-scope law (2026-07-16): Code repository is the explicit default for the project Git surface. Advertise Planning repository only when the CLI-selected planning root resolves to a Git identity whose canonical worktree top-level or common directory differs from Code repository; nested launch/planning paths inside one worktree collapse to Code. Every Git status, history, detail, patch, refresh, worktree removal, and handoff request carries `code | planning` scope through its URL, cache key, and server input. A Store identity does not imply a separate Git repository. Do not add Store clone/pull/push or automatic synchronization.
- Git repository-rebind law (2026-07-19): Git scope inventory is a reactive projection joined to the current Root Context, not a fixed query. Each backend-resolved Code/Planning binding carries an opaque, non-authorizing `bindingToken`; every Git query and mutation submits the expected token, and the Server compares it inside the current owner lease before any Git command, cache invalidation, refresh stamp, removal, or handoff. A new Planning-root service binding gets a new token even when it later returns to the same physical repository. Web query keys include both semantic scope and token, so A data cannot be relabeled or reused as B while B loads. During Root loading, stale refresh, failure, or projection mismatch, prior Planning data is display-only and Planning controls are locked; Launch-owned Code Git remains available when its Code projection is current. URLs persist only `code | planning`, never the transient token. A binding token is concurrency provenance, not a permission or repository path. `planningState` is a discriminated contract: `resolving` requires `planning: null`, while `settled` permits a nullable Planning descriptor; Git identity or owner-resolution failure is explicit evidence and must never be rewritten as a settled repository collapse. Git identity runners preserve stderr/exit/error classification: only explicit canonical `not a git repository` evidence may settle-null; unknown, empty, permission, executable, I/O, and canonicalization failures remain failure evidence, and Code failures are never mislabeled as Planning failures. Git handoffs additionally require a non-empty origin token and selector-matching `entityId`; family/token-only validation is insufficient even when the repository binding is unchanged. Dashboard Git snapshots carry the Code binding token observed with their entries; a later token may invalidate that snapshot but can never retroactively provenance it. Cached scope data is non-authoritative while its subscription is loading, errored, or reconnecting: a cache restore must not set the Git scope projection current before the first replacement emission. While the whole Git scope subscription is reconnecting with cached A, GitRoute and GitView overview/list/meta/files/patch queries, handoffs, refresh, pagination, worktree removal, and switching are locked until B is emitted; this does not block Code operations after a current Code-first emission when only Planning resolution is pending. Static Git projections use a distinct unavailable type with no live token and cannot submit Git RPCs.
- Git binding continuity correction (2026-07-19): Code-scope discovery and Code Git operations must not wait on a pending Planning-root manager transition. Git list/detail handoffs and View-Transition prefetch must carry the origin binding token; a handoff from A is rejected when B is current rather than being self-validated against the newly observed B token. This is a provenance/lifecycle boundary, not a UI-only loading concern.
- Git observation-authority correction (2026-07-19): A reconnect is a transport lifecycle, not merely a React remount. Git scope subscriptions consume the tRPC connection-state signal: cached data stays non-authoritative through `connecting` and `pending`, and only a replacement `onData` emission makes it current; `onError` never unlocks retained data. Dashboard refresh and worktree removal submit the binding token observed with the rendered snapshot rather than querying a later token at execution time, so an A-captured intent conflicts after B instead of being relabeled. The stable Code binding owner is backend/Launch scoped and is injected into both Git binding and Dashboard projection; it is not owned or publicly exposed by `PlanningRootServiceManager`. Combined scope resolution observes Code identity once per projection so a later Code failure cannot be caught and reported as a Planning failure.
- Git subscription generation law (2026-07-19): A Git projection callback is valid only for the active effect/transport generation. Retired `onData`, `onError`, connection-state, stop, complete, and static-loader callbacks cannot mutate React state or cache, and terminal error/stop/complete evidence remains visible and non-authoritative until replacement `onData`. The public Code-first Router stream must pass its first Code descriptor into the Planning reactive projection; a single public scope projection may not observe Code twice. Reconnect tests must prove real B query/action resumption, not merely disappearance of an error label.
- Root/Dashboard transport evidence law (2026-07-19): Root Context, Dashboard planning, and Active Changes are independent tRPC observables over one WebSocket handler; the transport does not promise cross-subscription causal ordering or provide a shared generation token. A typed public WebSocket harness that naturally delivers Root B before Dashboard B is characterization, not proof of a production race. Do not add a server generation barrier without a fixed-point causal red test and mutation-resistance evidence for the exact transition. Full local gates may run once focused lanes are green, but browser acceptance remains a separate completion condition.
- 6.11 decomposition law (2026-07-19): Git scope/token delivery, Project Binding settlement, Root/ActiveRoot reactive readiness, and the browser acceptance harness are independent work packages. Only the first is the Git checkpoint contract. Candidate A (pending Binding settlement) and Candidate B (Dashboard stale with offline WebSocket) must not be merged into one defect or used to authorize speculative production changes. A worker Goal names exactly one package, its typed fixed point, owner layer, and stop-loss after owner decision; an explicitly resumed staged Goal may sequence these packages, but each stage still requires its own red evidence and acceptance boundary.
- 6.14 dispatch-evidence ownership law (2026-07-21): `TerminalDispatchActions.actionsDisabled` is currently a shared semantic payload guard, not only a disabled-button projection. A caller-owned Compose generation assertion behind that guard is unreachable while `actionsDisabled` is true, even if a test mutates the rendered DOM button. Do not claim an exact caller-guard mutation red by weakening the shared guard, converting a button into a test-bypass form, or manually invoking `preparePayload`. If route-level mutation evidence must isolate the Compose assertion, first make an explicit product-contract choice: add a narrow caller-owned presentation-lock channel while retaining generic hard locks, or accept that the Compose assertion can be proved only at its typed owner boundary. Record and obtain the owner's decision before coding that contract.
- 6.14 Compose dispatch-instance law (2026-07-21): An open terminal-spawn dialog, its preset payload, and its expected Root generation belong to the Compose Root identity that rendered the dispatcher. Root A -> B must retire the A dispatcher at render/reconciliation time before a B-current dispatcher is exposed. A passive close effect keyed only by `interactionDisabled` is insufficient because immediately settled B preparation can batch away the intermediate disabled render. Same-generation `observedAt` refresh must preserve the instance. Evidence opens the real dialog under A, uses an immediately settled B rebind, proves A state is gone, and mutation-tests removal of only the reconciliation identity.
- React disabled-listener evidence law (2026-07-21): For an intrinsic React button rendered with `disabled`, removing only the DOM attribute does not make its production `onClick` listener reachable; React 19 also consults the committed props and suppresses the listener. Such a click can characterize presentation but cannot mutation-test a later semantic guard. Do not mutate React private node properties or split a production lock only to manufacture test reachability. Record the guard as source-level defense in depth, or make an explicit product-contract decision that creates a supported reachable boundary.
- Stage 1 decision (2026-07-21): Shared `TerminalDispatchActions` disabled cases remain presentation characterization plus reviewed `resolvePayload` defense in depth. Stage 1 mutation resistance is limited to the reachable Compose reconciliation identity and dirty-draft assertion; any public semantic hard-lock redesign is a separate Change.
- PTY slow-client isolation law (2026-07-22): PTY output fanout must bound each WebSocket client's queued bytes and terminate only the slow socket before its send backlog can starve the shared Server event loop. Reconnect replay is independently byte-bounded; socket termination keeps the Server-owned PTY process alive for list/attach recovery. Browser terminal output may update the renderer for every chunk, but React snapshots notify only on output inactive -> active and active -> idle transitions rather than once per chunk.
- Existing workflow-terminal reuse law (2026-07-22): Every PTY session carries immutable Server-stamped Planning Root provenance captured at creation (`null` for Launch), preserved through create/list/reconnect. OPSX may list an existing Launch Agent terminal as a valid Send target, or a Planning terminal only when its generation equals the prepared workflow generation. Workflow Send uses an acknowledged protocol; immediately before `session.write`, the Server resolves the current Planning owner lease and revalidates the request generation, while Planning sessions also require matching session provenance. Rejection or disconnect rejects the pending browser action without automatic retry or false success. Ordinary interactive keyboard `input` remains unchanged.
- Agent-terminal reveal law (2026-07-22): A successful Codex/Claude/Gemini create uses `createDedicatedSession` as the sole active-session owner, then reveals `/terminal` in whichever navigation area currently owns that route. A bottom-owned Terminal must call `activateBottom('/terminal')` even when its stored route is already `/terminal`, because route selection and panel expansion are separate state. A successful Send activates its existing target before the same reveal operation. The reveal helper does not create or relabel a session.
- PTY input-starvation law (2026-07-22): Configured Agent commands render to a structured invocation and spawn directly as `node-pty spawn(executable, argv)`; never create an interactive shell and inject the rendered command through `initialInput`. On Unix, all later PTY input uses one session-owned asynchronous file-descriptor writer that preserves byte order, handles partial writes, yields and retries bounded `EAGAIN`/`EWOULDBLOCK`/`EINTR`, caps pending bytes, and retires retries on exit/close. Do not call `tty.ReadStream.write`/`IPty.write` for Unix input: a long line sent before the child leaves canonical mode can fill `MAX_CANON`, busy-spin the Node main thread, and starve HTTP/WebSocket handling. Windows retains the native ConPTY writer. Workflow acknowledgement means the Server owner accepted the payload into this bounded writer; backpressure/closure rejects rather than reports false success.
- Live-status truth law (2026-07-22): The bottom/mobile `Live` indicator is a transport projection, not a memory of the last successful system payload. tRPC WebSocket `pending` means the socket is open; `connecting` means initial connection or reconnect is unavailable, and `idle` means closed. A reconnect with retained project metadata must immediately render disconnected/error styling and icon; only a current open transport plus system data may render `Live`. The status owner, not the icon component, must retire stale connected truth.
- 6.14 owner-acceptance law (2026-07-22): The owner accepted direct single-page and multi-tab behavior. Touching the current Planning root config may keep an open Dialog mounted while it shows `Refreshing planning root`, then preserve the same root. Changing Launch `store:` may likewise keep the Dialog mounted while refreshing, then replace the prepared prompt/target with the new root. Dialog auto-close is not an acceptance requirement; convergence to the correct current target is. Checkpoint closure still requires the independent status/typed-evidence corrections recorded in the Change.
- Interaction-latency diagnosis debt (updated 2026-07-31): The product exposes several independent correctness waits as Loading. First visits still have no retained display snapshot; detail View Transitions may wait up to 2.5 seconds for query prefetch; OPSX projections may await kernel warmup and CLI-backed Work. Planning-root cache misses no longer create one writer per subscriber or hold a write lock across `doctor/context`: same-generation callers join one transition flight, while dependency invalidation may still expose `refreshing` and temporarily revoke mutation authority. Preserve stale display where safe and retain current owner/generation gates; do not collapse these phases into one timing claim.
- CLI trace phase-precision law (2026-07-31): `cli.execute` duration is caller-observed response latency, not proof of child CPU/runtime duration. Runner resolution, synchronous spawn return, parent-observed spawn, first stdout/stderr, complete JSON, result delivery, termination request, actual `exit`, and actual `close` are independent events. Eager JSON delivery may return a successful result and request termination, but it must never fabricate an `exit` timestamp. Every CLI response span records event-loop active/idle/utilization so a delayed Node callback can be distinguished from an idle parent waiting on the child. A separate `cli.process` span remains open through real close whenever a child handle was returned, including eager response and asynchronous spawn failure. `cli.result.resolved` is the response-Span boundary: after it is delivered, late process events belong only to `cli.process`; writing them to the ended `cli.execute` span, or ending `cli.process` before its later close, is a lifecycle defect rather than harmless diagnostic noise.
- 6.16 Loading/authority sequencing law (updated 2026-07-31): Do not treat every visible Loading as one performance defect. `useSubscription` cold starts, cached Root Context remount/reconnect, pre-commit detail prefetch, root-action locking, and artificial mount-only route gates have distinct owners and contracts. Root cached-display/current-authority and navigation timing are accepted; live Settings and Archive no longer have artificial mount gates. Archive retains the real `isLoading && !archived` unknown-data wait and projects subscription-local recomputation as Updating without discarding retained data. Root generation/authority gates, Store/root provenance, Git tokens, compound Spec identity, subscriptions, and View Transition ownership remain non-negotiable; a Root refresh holding a write lock across CLI I/O or record retirement is forbidden.
- Archive list topology law (2026-07-22): Archive transport errors are real page facts. With no data, render the raw error without claiming an empty Planning root; with retained data, render the same error beside the stale Archive rows and preserve their links. Only `archive.subscribe` uses the opt-in typed projection lifecycle: initial no-data remains Loading, `recompute-started` retains A as Updating, replacement data/error clears Updating, retired live/static generations cannot publish, and static mode never reports Updating. A retained empty snapshot is not current-empty truth: `[] + Updating` shows only the updating fact, while `[] + settled` may show `No archived changes yet.` Do not fabricate Updating from `data + isLoading`, WebSocket state, or runtime invalidation.
- Change-list workflow-status topology law (2026-07-22): Change metadata/tracked-task progress and CLI workflow Status are independent projections. Preserve Change rows while Status settles. Show `Loading workflow status` only while the Status subscription is actually in its initial no-data loading state; a terminal Status error renders its raw error and ends Loading, while a current Status list without a matching Change renders `Workflow status unavailable`. Absence of one matching Status is not perpetual loading, workflow completion, or archive readiness.
- Change-list main-error topology law (2026-07-23): Main Change rows and the per-row workflow Status remain independent projections. A terminal main Changes error ends only the main-list Loading/empty claims: with no rows, render its raw alert and no list frame; with retained rows, preserve rows, tracked progress, links, and Status evidence beside the alert; with retained `[]`, suppress the active-empty conclusion. Do not manufacture an Updating state or change the accepted Status topology.
- Context terminal-error precedence law (2026-07-23): Root Context projection state and WebSocket transport lifecycle are separate facts. A terminal transport error dominates a retained/no-data `state=loading` projection for page presentation: render the raw error and do not simultaneously claim `Loading context`. This is presentation precedence only; preserve the authoritative subscription, cached-display/current-authority gate, refreshing/stale CLI evidence, and Root mutation lock.
- Spec-list transport-error topology law (2026-07-23): Spec Catalog transport error and per-Reference Store diagnostics are distinct facts. With no Catalog, render the raw transport error without Catalog tabs or Owned/Referenced empty claims. With retained non-empty Catalog data, render the same error beside the retained source-distinct rows/links. While transport error is active, suppress source-empty conclusions; preserve compound identity, read-only Reference evidence, and internal Store diagnostics. Do not infer Updating or current emptiness from generic retained cache.
- Config Schema-files projection law (2026-07-23): The Config Schema workspace contains independent `configBundle`, `schemaFiles`, templates, and template-contents projections. Only the Read/Edit file panel consumes `useOpsxSchemaFilesSubscription(selectedSchema)`. Its no-data initial loading renders `Loading schema files...`; a terminal error renders its raw alert and suppresses FileExplorer empty conclusions whether data is absent or retained as `[]`; only current `[]` with no error may use the existing empty state. Preserve retained non-empty tree/editor data beside an error. Do not make Schema-file error handling a pretext to alter Preview, root authority, mutations, Router, static data, or the other Config projections.
- Config Schema-catalog/tab-inventory law (2026-07-23): `useOpsxConfigBundleSubscription()` owns the Schema tab inventory, not the selected Schema file projection. A Schema loading/error/empty host must remain mounted when the inventory has no tab: `undefined + loading` shows only Schema-workspace Loading; terminal absent/empty errors show a raw alert without a Schema tab or empty claim; retained non-empty inventory keeps existing Schema tabs/content beside the alert; only settled `[] + no error` shows `No schemas available.`. Do not place this status only inside a Schema tab, because no tab exists for the states it must explain. Preserve the three fixed Config owner tabs and do not change K's Schema-files owner, templates, Preview, Root gating, mutations, Router, Server, static provider, or generic subscription lifecycle.
- Subscription lifecycle-owner law (2026-07-23): `useSubscription`, `useReactiveProjectionSubscription`, and `useAuthoritativeSubscription` share one internal lifecycle owner for cached snapshots, effect generation, subscription retirement, and static-loader publication. A callback or loader from retired generation A must not write React state or cache after B begins or after unmount. This extraction preserves each public state contract: ordinary cache-rebind policy, opt-in recompute `isUpdating`, and authoritative transport/operation gates remain distinct. It does not add generic Updating, change page presentation, alter tRPC/Router/Server, or relax current-authority locks. Prove the common retirement transition with live and static red cases before another page adopts lifecycle behavior.
- Reactive recompute lifecycle law (2026-07-22): Filesystem invalidation, WebSocket transport state, recompute start, and replacement data are separate facts. Preserve `ReactiveContext.stream<T>` and the existing `createReactiveSubscription<T>` raw-data contract. Add lifecycle observation as an opt-in path: no start event for the initial task, exactly one `recompute-started` after a dependency wake and abort check but before each real rerun, then one data event after completion. Do not derive this event from runtime-facet invalidation or transport `connecting/pending`; neither proves that this subscription consumed the invalidation. Migrate one Router endpoint at a time only after the Core/Server ordering contract is independently accepted.
- Parent-inventory rebind law (2026-07-28): a bounded artifact projection must not register one timer or watcher
  dependency per absent leaf directory. Read the existing parent inventory first, then subscribe progressively to
  each child only after that child exists; `.codex -> skills -> workflow` creation therefore rebinds through
  objective parent entries even when Linux/CI coalesces deep creation events. Explicit one-shot reads must retire
  the parent cache too. Evidence stages each directory creation, mutation-removes the parent dependency, and keeps
  empty-installation missing-path polls at zero; a generic `reactiveReadDir` poll fanout is forbidden.
- Reactive subscription retirement evidence law (2026-07-22): An unsubscribe test that triggers invalidation and immediately inspects delivered events does not prove the Server aborted its Core stream; the second task may start on a later microtask, and observable delivery behavior can mask owner leakage. Prove the tracked `ReactiveState.subscriberCount` moves from one to zero, record replacement task entry/run count, settle the deterministic promise chain, and mutation-test removal of the exact `controller.abort()` cleanup. Core abort evidence and observable no-delivery evidence are different facts.
- Focused Vitest invocation law (2026-07-23): `pnpm --filter @openspecui/web test -- <files>` forwards a delimiter through the package script and can run the entire unit project, including unrelated flaky/slow fixtures. For a bounded review slice, invoke the unit project directly as `pnpm --filter @openspecui/web exec vitest run --project unit <files>` and record that exact command; do not relabel an unrelated full-project timeout as current-slice evidence.
- Heavy interaction-suite budget law (2026-07-28): a large jsdom suite may declare one bounded suite-level
  execution timeout only after workspace-serial evidence shows resource contention rather than a production
  regression: different cases cross the default threshold, the exact cases replay well below it, and assertions
  remain unchanged. Never widen `waitFor`, transport, mutation, or user-visible lifecycle deadlines to make the
  suite green; record the focused replay and full-suite result separately.
- Settings artificial-mount-gate law (2026-07-22): Live Settings has no route-level readiness fact beyond its owned subscriptions and immediately available preferences. Do not hide the entire route behind a local `useState(true) -> useEffect(false)` first-frame gate. Removing that gate also makes first-render draft correctness a production boundary: when cached Config is already present, every writable local draft must initialize from that Config instead of exposing a default value until a passive effect catches up. Later synchronization is field-value owned: a newly parsed but value-equal Config object cannot clear a dirty draft, while a real upstream field change replaces only that field. Prove live composition, non-default cached drafts before effects, value-equal emission preservation, and real field replacement; preserve static Appearance-only composition and every loading/updating/error state owned by `use-settings-tool-subscriptions` or another real data owner. Archive's separate `requestAnimationFrame` gate requires its own package and evidence.
- 6.16 detail-navigation timing law (2026-07-22): Measure the existing detail-navigation path at its production coordinator before changing its wait policy. One bounded, typed, process-memory trace owns `requested -> prepare-settled -> route-update-issued -> transition-settled | failed` with attempt id, route area, origin path, target path, outcome, and monotonic phase durations. The synchronous View Transition update callback is only update-issued evidence: a discarded TanStack `navigate()` Promise cannot prove Router or DOM commit. A newer request in the same area marks the prior sample superseded; late A effects remain attributed to superseded A but cannot mutate B or become the latest-request evidence. Do not persist, upload, notify, or print traces. Subscription-first-data and Root-ready timings remain independent future observations until an explicit protocol token can correlate them; timestamps alone cannot claim causality.
- 6.14 New form-submit law (2026-07-21): OPSX New's disabled Create button is a freshness projection, while the mounted form `onSubmit` is the dispatch owner. A stale prepared command must be rechecked against `rootActionRef.current` immediately before `createDedicatedSession`. Evidence submits the real form while the stale button remains disabled; mutation removes only that inline owner guard and must not mock or weaken the shared `isWorkflowTargetCurrent` used by presentation. Same-generation `observedAt` refresh remains dispatchable.
- 6.14 decomposition reset law (2026-07-21): OPSX New, Propose, Compose, and automation are independent packages, not one acceptance unit. Each worker Goal names exactly one production owner, one precise mutation red, and one corrected green case; focused review must accept that package before the next package starts. A passing test that replaces a real dialog/button, manually invokes a downstream mock, or removes both presentation and dispatch guards is invalid evidence. The revised order is Compose pending-generation ownership, New real form-submit guard, Propose real Dialog-to-`createShellSession` chain, then Terra focused automation. Full gates and owner browser acceptance remain outside all worker packages.
- 6.14 asynchronous Compose ownership law (2026-07-21): A dirty Compose draft belongs to the Root generation that captured the edit, even when preparation for Root A is still pending while Root B becomes current. B preparation may settle before A; a late A completion must not overwrite B's target, clear the draft, or relabel the draft as B. Same-generation `observedAt` refresh preserves the existing editor/dialog instance. Prove this through the real mounted Compose route and typed preparation boundary; do not bypass a disabled fieldset or call `preparePayload`/terminal mocks manually. Mutation-resistance must remove only the generation/reconciliation transition that retires the stale A completion and show the same test fail.
- Independent-observable evidence law (2026-07-19): HTTP and WebSocket readiness timestamps are local observations, not a cross-transport happens-before contract. Tests must not assert `ws.observedAt >= http.observedAt` or use arbitrary sleeps/retries to manufacture ordering; compare each channel's typed payload/readiness and record causal ordering only where the protocol provides an explicit token or sequence.
- Pinned browser-fixture law (2026-07-19): Readiness evidence must execute the direct pinned OpenSpec CLI executable with an explicitly isolated `XDG_DATA_HOME`; `npx` runner resolution is network-dependent and can produce a false cold-start timeout. Remove proxy variables from bounded local probes. Classify that timeout as fixture/network evidence, not a server startup or transport defect, and do not add a production workaround for it.
- Pinned CLI clean-build law (2026-07-20): `references/openspec` pins source at exact v1.6.0 while upstream ignores generated `dist`; a local `dist` is non-authoritative evidence. Any clean CI lane that directly executes `references/openspec/bin/openspec.js` must initialize the gitlink, verify exact SHA `e1b51d111ab446b54dee2d6159ac245f0339ae52`, install the nested lockfile without outer-workspace coupling or lifecycle scripts, explicitly build, and assert `dist/cli/index.js` before running pinned integration fixtures. Do not add this preparation to Browser shards that never execute the pinned CLI.
- Dashboard projection law (2026-07-16): Dashboard Spec, Change, task, and workflow metrics come only from the CLI-selected Planning root. The page names that path, root source, optional Store id, and direct Reference diagnostic counts without inferring Reference health or completeness. Its `Code Git Snapshot` remains Launch code-repository scoped; a distinct Planning repository is shown independently and never substitutes for Code activity. Formal workflow completion may be labeled `workflow-complete`, but only CLI validate/archive outcomes may claim archive readiness.
- Change list law (2026-07-16): Active Changes come only from the current writable Planning root; Launch-only and Reference content never enter the list. Workflow state consumes the full formal `TrackedTaskPhase`, not a derived completion boolean: `no-tasks` renders explicitly, only `complete` may contribute to `Workflow Complete`, and neither status claims archive readiness.
- Change detail law (2026-07-16): Reactive Status, artifact Instructions, and Apply Instructions use typed CLI contracts with the Root Context-derived explicit Store selector. Live `ChangeStatus.provenance` preserves `planningHome`, `changeRoot`, artifact/resolved/existing paths, next steps, action context, and root source; static provenance is explicitly unavailable. Change detail shows those paths plus direct Reference diagnostics without inferred health/completeness. `Update` remains a first-class action. Archive is never authorized from `status.isComplete`; it opens the CLI dialog, runs strict validation, stops on failure, and preserves validate/archive diagnostics without synthesized retry.
- Archive surface law (2026-07-17): Archive query/detail/subscription data comes only from the current writable Planning-root services. Launch-only archives and References never enter the list. The UI names that scope explicitly. One Server-owned strict archive stream resolves Root Context once, runs `validate --strict`, and only then runs archive with duplicate validation disabled; an operator may explicitly skip validation. Every client-supplied Archive `changeId` passes the shared canonical entity guard before Root Context resolution or CLI execution, including the explicit no-validation path; upstream validate/archive behavior is not a path-confinement boundary. The browser never sends a Store selector, and any nonzero/indeterminate preflight outcome terminates without starting archive or a synthesized bypass retry. Public OpenSpec execution cannot be secured by denying only the latest discovered subcommand: generic RPCs are read-only/explicitly allowlisted, while every root/environment mutation delegates to a typed Server owner. Any retained generic argv policy uses the repository-standard strict `yargs` parser and pinned OpenSpec evidence; do not maintain a handwritten parallel parser, and remove the generic capability when no production caller requires it. Fixed global installation has its own argument-free route. The direct `change.archive`, `cli.archive`, `cli.archiveStream`, Adapter rename, generic `cli.execute`, and arbitrary-command stream paths are absent. Archive diagnostics and no-retry behavior must be tested through `archiveStrictStream`, never a mocked generic route. PTY remains an explicitly user-controlled shell, not an application Archive mutation RPC or command sandbox.
- Spec list law (2026-07-16): `/specs` defaults to Owned Specs from the writable Planning root. Referenced Specs appear only in an explicit sibling view grouped by exact Store id, remain visibly read-only, and preserve compound identity in links and View Transitions. Empty Reference copy says only that no Referenced Specs are currently observed; it never claims machine-wide completeness.
- Search source-isolation law (2026-07-18): Project Search has exactly `active-root | referenced-specs`; the default Active root contains only Planning-root Owned Specs, Changes, and Archives, while the explicit Referenced scope contains only direct read-only Referenced Specs. Filtering occurs before matching/scoring/sorting/limit in the shared engine. Generic Search types may keep scope optional for unrelated consumers, but project documents and hits require one exact scope at the Server/Web boundary. A backend without the source-aware `search.subscribe` contract must fail closed; never call an older mixed `search.query`, relabel its limited hits, or infer truth from an empty response. The first reactive query initializes from current Planning-root truth before searching; deferred warmup is only an optimization. Every public Search query and every subscription rerun reads current documents before returning, and every subscription must collect those file dependencies inside its own `ReactiveContext`. Awaiting a warmup or rebuild Promise whose reads began in another context does not transfer dependency ownership; only provider mutation/search may be serialized after caller-local collection. Until `7.*` legally extends `ExportSnapshot`, static Specs are Owned-only and Referenced Search is a neutral empty projection; tests must not use type escapes to pre-materialize future References. The current Search URL is the render-time source authority: browser history or another external location update must retire the old scope before any render can expose its tab, request, or hits. Duplicated local scope reconciled only by a passive effect is not source isolation.
- Terminal cwd-target law (2026-07-16): Every PTY creation explicitly selects `launch-project | planning-root`; clients never submit arbitrary cwd paths. The backend resolves Launch from its configured project and Planning from a current CLI-owned Root Context before spawning. Planning creation remains locked during Root Context loading, refresh, or failure. Create/list/reconnect responses preserve the semantic target plus server-resolved `initialCwd`, and terminal tabs display both. Both targets inherit the same backend process environment, including `XDG_DATA_HOME`; do not load project `.env` files or mutate environment by cwd target.
- Terminal cwd-owner evidence law (2026-07-20): evidence claiming Server-owned Planning cwd resolution must cross the production composition that connects `createWebSocketServer` to `PlanningRootServiceManager.runOperation`. Injecting a hand-authored `withCwdTarget` proves only the downstream PTY handler/Manager contract. The focused test must fail when the production Planning branch is mutated to Launch cwd, then pass after restoration; otherwise checkpoint 6.12 remains open.
- Root-dependent mutation law (2026-07-17): Spec, Change, task, archive, entity-file, artifact-output, schema, Template, and Active Root Config mutations execute inside a manager-owned operation lease for the current Planning root; resolving a raw mutable service and using it after leaving the transition boundary is forbidden. Root replacement waits for every admitted A operation to settle before retiring A and exposing B, while new operations queue behind the transition. A stream cancellation request is not settlement: only child `close` or a startup result proving no child exists releases its lease. A forced-termination timeout is a bounded termination failure: it keeps A blocked rather than exposing B, but must produce exactly one observable terminal error so no client remains `running`. Backend disposal stops new admission, requests cancellation from retiring active streams outside any transition that waits on their leases, and resolves or rejects in bounded time without depending on client detach or a future CLI event. Launch-owned Project Binding and Code Git remain explicitly separate owners. Every public write/create/delete path, not only Adapter methods, must use its physical/reactive owner; an audit that ignores existing Router mutations is incomplete. Every client-supplied Spec id, Change id, and entity-relative file or glob path must pass the shared canonical entity/schema guard before any filesystem read, watcher dependency, `mkdir`, write, rename, or remove. Lexical containment is insufficient: resolve existing ancestors physically and reject symlink traversal outside the owned root before mutation. Every successful direct filesystem mutation settles reactive file/directory/existence/stat projections before returning; watcher delivery is fallback convergence. Each formal tracked task carries its exact source `filePath` plus file-local 1-based checkbox index; task mutation must use that identity and never fall back to a fixed top-level `tasks.md`.
- `packages/app` owns persistent project-backend connections, runtime-environment identity, and cross-project entry points. One App tab hosts one project backend; App does not duplicate Change, Spec, Config, Git, or Terminal workflows.
- `packages/web` owns one launch project's workspace and every operation against that project's single CLI-resolved planning root. References extend its read-only spec context; they do not create multiple writable roots inside one project tab.
- Store mutation is OpenSpec runtime-environment scoped, not project scoped. App-native Store management requires a selected online environment and a backend protocol that exposes stable environment identity and CLI-backed Store capabilities. The project Web surface may show Store and Reference diagnostics but must not imply that its registry is project-local.
- Store Manager selection surface law (2026-07-26): Store Inspector, Inventory, and Context share the App's one persisted `activeTabId`; Store Manager must visibly expose and update that exact selection while retaining every other connected backend. Sessions tab selection and Store Manager selection are two controls over one state, never independent owners. Store reads and mutations may use only the selected tab's current observation generation; a first-online fallback remains forbidden.
- Decision (2026-07-15): `--app` remains an explicit experimental mode, so production Store Manager is not a 6.0 release gate. A throwaway Store Manager prototype is nevertheless required before formal specs converge because environment-scoped Store administration closes the App product story.
- Implement the eventual Store Manager as App-native routes inside `packages/app`, alongside Home/Connections, Environment, and App settings. Do not add extra HTML entries to `packages/web` or extract a new package until a stable environment/Store protocol module is genuinely shared by multiple consumers.
- Decision (2026-07-15): Store Manager uses the Store Inspector (`B`) as its primary interaction, the Context Matrix (`C`) as a sibling Context view, and the Registry Table (`A`) as a wide-screen Inventory view. Do not collapse Root/Reference relationships into the destructive-operation inspector or make the table the only navigation model.
- Decision (2026-07-15): Any connected backend that satisfies the hosted protocol may participate in Store Manager; do not restrict the domain model to loopback backends. OpenSpec remains local to the backend host and its inherited data scope. App must not infer trust from URL locality or implement registry semantics itself.
- Decision (2026-07-15): The hosted protocol names runtime-environment identity `envUri`. It is a backend-issued, opaque, non-dereferenceable URI identifying the combination of backend host identity and effective OpenSpec data home. Multiple backend processes, projects, ports, and API URLs share one `envUri` when that pair is unchanged; changing host or data home changes it. `apiBaseUrl` remains the backend-instance locator, and App must neither construct `envUri` nor expose raw host/data-home values through it.
- Decision (2026-07-15): OpenSpecUI may optionally protect one backend with a single shared **Backend Access Gate** enabled by CLI `--auth` or `--password`. This is not a user, role, ACL, permission, or multi-tenant system. Without either flag, the current unguarded behavior remains unchanged.
- The Access Gate has one canonical credential form: `Authorization: Bearer <credential>`. `--auth` generates a high-entropy credential and prints the complete header; `--password` normalizes an operator-supplied secret into the same Bearer form. Prefer a hidden prompt when `--password` has no inline value; warn that `--password=...` can leak through shell history and process inspection.
- When enabled, the Access Gate covers the whole backend boundary: `/api/*`, HTTP tRPC, tRPC subscriptions, PTY WebSocket, files, terminal, notifications, and future Store operations. HTTP uses the Authorization header, tRPC WebSocket uses connection parameters, and PTY WebSocket authenticates in its first message before any command is accepted.
- Capability discovery only states which hosted-protocol features a backend implements; it never grants access. App may receive an auto-launched credential once through a URL fragment, must remove the fragment immediately, and must keep the credential in session memory rather than query parameters, persisted tabs, or `localStorage`.
- Browser credential bootstrap ordering law (2026-07-26): Project Web must consume and strip the launch fragment before importing or initializing any App-owned navigation, URL canonicalization, health probe, HTTP/tRPC client, WebSocket, PTY, or raw-resource owner. Root-route normalization may preserve public `api`/`session` query parameters but cannot be the credential consumer. Regression evidence must import the real root `entry-client`, cross the App-owned navigation boundary into the first authenticated request, and fail if eager `App` import is restored; a pre-normalized route or direct helper test is insufficient.
- Canonical embedded entry law (2026-07-26): Server health owns `embeddedUiUrl` and must advertise a directly renderable canonical Project Web route (`/dashboard` for the current shell), so App iframe launch does not visibly settle through a second root-to-route history transition. App treats this URL as opaque backend metadata and must not append or infer Web routes. Evidence must cross the typed health response and verify the advertised pathname; an App fixture that authors `/dashboard` itself cannot prove the Server contract.
- App credential convergence law (2026-07-26): Launch relay ownership belongs to the App route lifetime, never the Sessions/iframe route. Every live same-origin App window absorbs each newly relayed locator credential before leadership handling; the leader's directed acknowledgement returns an immutable snapshot of its current in-memory locator bindings so a newly opened window can authenticate persisted older tabs. Credentials remain absent from query parameters, shell state, localStorage, and logs. The periodic health refresh may expose a missing binding as 401 but is never interpreted as credential expiry. Evidence requires an A/B/C relay red/green test, mutation failure when acknowledgement snapshot transfer is removed, and a real Router test proving launch reception while Environment is mounted.
- The Access Gate does not provide transport confidentiality. Any non-loopback deployment protected this way requires HTTPS/WSS; exposing the credential over plaintext transport is unsupported.
- Decision (2026-07-15): Store protocol discovery uses three product-level capabilities: `stores.inspect` for list/detail/doctor projections, `stores.mutate` for setup/register/unregister/remove execution, and `contexts.inspect` for project Root/Reference relationships. Do not mirror every CLI subcommand as a capability.
- Runtime capabilities are objective compatibility facts, not permissions or inferred workflow state. Protocol-version requirements decide whether a backend can connect; optional capabilities decide which surfaces can render. Operation applicability and failure reasons must come from CLI results and diagnostics rather than additional frontend capability inference.
- OpenSpecUI is an objective visual projection of OpenSpec. Preserve upstream field meanings, diagnostics, exit status, and uncertainty; presentation may summarize facts but must not silently invent health, ownership, completeness, synchronization, or authorization conclusions. A typed CLI transport is the single source for both execution and displayed command evidence; never pair it with independently caller-authored `command/args` that can describe a different operation. Prefer the backend-emitted effective command when runner resolution or Root Context adds arguments.
- Decision (2026-07-15): Store Inventory projects `openspec store list --json`, Store Inspector projects `openspec store doctor [id] --json`, and each project Context projects `openspec context --json`. Hosted envelopes may add provenance such as `envUri`, CLI version, observation time, and exit status, but must not replace or reinterpret upstream payload facts.
- Store Doctor optional-input law (2026-07-26): an all-Store Inspector request preserves the absent upstream `[id]` as absent tRPC input, never explicit `null`; the optional Router accepts absent input or an object, while `null` is a contract error. An explicitly supplied Store id is preserved by presence rather than truthiness and remains CLI-owned validation input.
- Context Matrix is an observed-only App projection joined by `envUri` and Store id across online connected projects. It is not a machine-wide reverse index. Say "observed references" or "no reference currently observed", never "all references" or "unreferenced". Offline projects are unknown unless a last-observed snapshot is shown explicitly with its timestamp and stale state.
- Do not scan the backend filesystem for projects or create an OpenSpecUI-owned project-to-Store registry. Such discovery would invent completeness and ownership semantics absent from OpenSpec.
- Decision (2026-07-15): Store mutations are backend-owned operations with `accepted -> running -> succeeded | failed` lifecycle. Client disconnect only detaches observation; it does not kill the CLI. Loss of a recoverable terminal result is `indeterminate`, never fabricated as failure or cancellation. V1 exposes no mutation Cancel and performs no automatic mutation retry.
- A client request id deduplicates mutation starts within one backend process. Final results preserve CLI JSON, diagnostics, stdout/stderr, and exit status. Every terminal or indeterminate outcome invalidates affected projections before they are pulled again.
- The reactive kernel is OpenSpecUI's primary consistency enhancement over OpenSpec. Changes produced by CLI mutations or external filesystem activity must invalidate the affected `envUri` Store/Context facets; all subscribed clients then pull fresh CLI-owned projections. Push notifications carry invalidation, not a competing copy of Store truth.
- Upstream evidence discovery law (2026-07-26): Before implementing or optimizing an OpenSpec CLI-backed projection, inspect the exact pinned official OpenSpec source and executable flow to identify the physical trigger sources that can change that CLI result: files, directories, registry/config paths, resolved globs, and content structures. Do not guess dependencies from UI fields, command names, or existing OpenSpecUI polling code. This map describes invalidation dependencies only; it never authorizes OpenSpecUI to replace the CLI with a parallel domain parser.
- CLI-backed reactive Work law (2026-07-26): The cached business value of an OpenSpec projection is exclusively the latest settled typed OpenSpec CLI result with its raw evidence, diagnostics, and exit status. One idempotent CLI Work dynamically collects file, directory, invalidation-token, and content-derived trigger dependencies through reactive reads or explicit observation leases. Parsed trigger content may add or remove observation paths, but it must not become the projected business result. A matching trigger marks the settled CLI snapshot `stale-display-only`, publishes `revalidating`, reruns or joins the same single-flight CLI Work, and atomically replaces the cache only after the new CLI result settles. Failure retains the old snapshot as `refresh-error`; it never relabels stale data as current.
- CLI projection push/pull law (2026-07-26): Server push carries invalidation and Work lifecycle only; clients pull the corresponding typed CLI projection. While revalidation is running, clients may render the retained settled snapshot plus the separate updating state, but no stale snapshot may authorize a mutation. Periodic polling must not be the normal discovery or freshness mechanism; it is permitted only as a bounded, explicit degraded fallback when a watcher, missing path, platform, or upstream substrate cannot emit usable change evidence.
- Admission Pull law (2026-07-31): Every admitted live Push/Pull projection establishes its lifecycle subscription and immediately starts one typed HTTP Pull without waiting for the first lifecycle notice. A lifecycle notice replayed synchronously during subscription setup owns that admission Pull and suppresses the redundant notice-free Pull; every later notice starts a replacement Pull and advances request generation so an older response cannot overwrite it. Disabled or unmounted hooks remain unadmitted and start no Pull.
- Readonly observation refresh law (2026-07-31): A refresh that only reacquires objective state remains a read query even when its implementation maintains an internal cache, invalidation token, or refresh stamp. Those auxiliary writes are not domain mutations and must not place the request in the mutation/write admission lane. Route mount, window focus, and visibility transitions do not independently invalidate Git. A user-selected timer owns one absolute `nextRefreshAt`: hidden documents clear the physical timer but retain the deadline; visibility resumes the remaining delay or requests one coalesced refresh when overdue. Manual refresh and an accepted automatic refresh rebase the deadline. Same-identity Projection Work invalidation cancels the owned Git subprocess before the replacement may consume the single Git resource slot; binding provenance prevents stale publication. Domain-changing commands such as config writes, model downloads/deletes, worktree removal/switching, Apply, and Archive remain mutations.
- Planning-root transition single-flight law (2026-07-31): A missing current Root snapshot creates or joins exactly one transition flight keyed by the complete `project/stores/context` invalidation identity before any write-lock admission. CLI availability, Doctor/Context, reactive dependency tracking, admitted-operation drain, stream settlement, record disposal, and record construction run outside the transition lock. Different generations may resolve independently; commits remain serialized and compare the captured generation before detaching or installing a record. A late A result is discarded and its caller may adopt already-current B. The write lock covers only short active-record/snapshot state commits, while operation admission uses a short read lock. Readonly projection refresh remains read-domain work even when it joins this lifecycle transition.
- Read/write lock trace law (2026-07-31): Every Planning-root write-lock admission carries an explicit stable source name and a captured source stack. Its OpenTelemetry span begins before queueing and ends only after release, with timestamped `write-lock.queued`, `write-lock.acquired`, and `write-lock.released` events plus wait/hold durations and queue-state counts. A read queued by writer preference records every active or earlier queued writer under `lock.blocked_by.*`; the enclosing `trpc.*`/projection span remains the request-level source. A lock trace that starts only after acquisition cannot diagnose the blocking interval and is invalid evidence. A Root commit may publish only after the complete invalidation identity still matches; record identity alone cannot make stale A current. Dashboard readonly refresh controls lock only for its own query Promise; unrelated shared Git Projection Work may show updating evidence but cannot retain the manual action lock.
- The reactive observation domain must include the effective OpenSpec data home and dynamically CLI-discovered Store roots in addition to the launch project. The Store fallback may check watcher health at a bounded interval, but it must remain inert while observation is healthy and must never execute the CLI or become projection truth.
- Reactive observation ownership law (2026-07-16): One `ReactiveObservationEnvironment` owns the dynamic root set for one backend runtime environment. Repeated logical acquisition is reference-counted inside the environment; physical watchers are reference-counted across environments and close only after the final lease is released. Path subscriptions exist independently from roots, bind to the deepest active containing root, and automatically rebind when roots appear, disappear, or recover. Server runtime owns the Launch lease, each planning-root service record owns its Planning lease, and teardown releases both. Do not restore the single-root `initWatcherPool` or `getWatchedProjectDir` model. This root lifecycle is infrastructure only: data-home facets, Store-registry reconciliation, invalidation identity, and polling fallback remain separate contracts.
- Data-home invalidation law (2026-07-16): One `OpenSpecDataHomeObserver` owns the effective inherited data-home lease and maps external filesystem changes to exactly four runtime invalidation facets: `stores`, `worksets`, `schemas`, and `context`. A token carries only facet identity plus a monotonic generation; it never carries registry, Workset, schema, or Context payload truth. Store subscription, Root Context/planning resolution, and Opsx schema projection respond by pulling fresh CLI-owned data. The existing Store timer remains temporary fallback until checkpoint 4.8; do not describe it as the data-home primary path or remove it before fallback policy is bounded.
- Missing-path ancestor settlement law (2026-07-28): `ProjectWatcher` ancestor matching is default-off and may be enabled only for a bounded logical target whose owner verifies actual target presence. While that target is absent, one coalesced strict-ancestor creation event may wake its subscription; unrelated ancestor activity emits no domain invalidation. Once the target exists, matching returns to the normal exact path/subtree contract, and deletion re-arms creation settlement. `WatcherPool` cache identity includes this option. OpenSpec data-home observation enables it only for `stores/registry.yaml`, `worksets`, and `schemas`; it does not introduce leaf polling or broaden ordinary project subscriptions.
- Store observation reconciliation law (2026-07-16): `StoreObservationService` reconciles Store-root observation and `stores/context` invalidation leases only from a successfully executed and typed `openspec store list --json` projection. Store id is the reconciliation key and CLI-returned `root` is the observed path. Added ids acquire both leases, moved ids replace them, removed ids release them, and a successful empty list releases all Store roots. Command failure or incompatible payload performs no reconciliation and therefore never fabricates an empty registry. Watcher acquisition failure records an observation gap and retries on the next successful list; it must not change the CLI Store projection or retain a stale old-root lease.
- Project-root invalidation law (2026-07-16): One environment-local `RuntimeInvalidationIndex` is shared by data-home and project-root observers. `RuntimeRootInvalidationRegistry` normalizes and reference-counts Launch and connected Planning roots separately from physical watcher ownership; the same canonical Launch/Planning path retains one recursive callback. Filesystem activity on either root invalidates only `project` and `context`, and consumers pull fresh CLI-backed projections. Each planning-root service record releases its invalidation lease on disposal, while backend teardown releases the Launch registration before the observation environment. Do not expose invalidation reader methods through `OpenSpecDataHomeObserver` or create per-service invalidation indexes.
- CLI mutation invalidation law (2026-07-16): OpenSpec command-to-facet classification is backend-owned. `CliMutationInvalidator` invalidates affected facets exactly once before returning buffered success/failure, rethrowing an indeterminate error, forwarding a streamed terminal/null exit, or invoking cancellation after client detach/startup failure. Read-only, help, and unknown commands do not invent affected facets; clients never submit facet identities. Generic non-OpenSpec command streams remain outside this mapping. This settlement boundary does not replace the later backend-owned Store mutation lifecycle or make Store operations client-cancelable.
- Invalidation push law (2026-07-16): `runtimeInvalidation.subscribe({ facets })` emits only `RuntimeInvalidationToken[]` with facet identity and generation. Store clients pull `stores.list` after tokens; no push route may carry Store/Context/registry/path/health payloads as a competing truth source. The temporary Store reminder is identity-only and remains bounded work for checkpoint 4.8. Root Context keeps its separate typed full projection subscription required by checkpoint 3.2.
- Invalidation coalescing law (2026-07-16): `RuntimeInvalidationIndex.invalidate` advances generations and reactive dependencies synchronously, then batches direct-listener delivery at the microtask boundary. Each subscribed listener receives at most one latest token per facet in deterministic facet order for that turn; releasing a listener removes its pending batch. Listener failure is isolated. Client projection pulls use request generations so a slower older response cannot overwrite newer CLI truth.
- Bounded Store fallback law (2026-07-16): Healthy runtime invalidation subscriptions have no polling timer. One backend-owned `StoreObservationFallbackService` checks at a bounded interval and emits identity-only `stores/context` invalidation only while data-home observation is failed, successful CLI Store truth has an unobserved desired root, or an environment watcher is absent, uninitialized, or residency-evicted. It retries data-home observation but never executes or parses Store CLI itself; client pull plus `StoreObservationService` reconciliation remains the retry path for Store roots.
- Reactive teardown law (2026-07-17): Runtime close is one idempotent asynchronous path. Stop and await the bounded fallback first; then release planning-root services, Store leases, data-home observation, project invalidation registrations, and the observation environment. Root Context replacement/removal must drive Planning-root manager transition itself; it may not wait for an unrelated later Planning-root RPC. Before clients can use root B identity, root A operations have settled and root A observation/invalidation leases, Kernel, hooks, Search, Dashboard, preview sessions, and mutable service capabilities are retired. A previously returned Adapter or service handle cannot remain a usable write capability after replacement. Subscriber disconnect removes pending coalesced delivery. Fallback disposal waits for in-flight checks and may not emit after teardown. Completion requires zero physical watcher roots and zero path subscriptions.
- Multi-client convergence law (2026-07-16): External Store-root edits and data-home registry edits, concurrent CLI mutation settlement, reconnect, and root disappearance must converge through one environment-local invalidation index. All connected clients receive the same latest identity batch; reconnect starts from current generations; disconnected clients receive no pending batch. Store-root observation without a matching `stores/context` invalidation lease is incomplete.
- Normally, one endpoint's successful change reaches other endpoints sharing the same `envUri` before their next action. Only genuinely concurrent submissions remain for OpenSpec registry locking and diagnostics to arbitrate. Duplicate invalidations are harmless and must be coalesced or treated idempotently.
- App replacement-observation single-flight law (2026-07-28): Initial tab admission remains independent. After admission, overlapping explicit refresh and reconnect requests for the same complete Hosted tab identity join one replacement observation before allocating another generation or health probe; repeated reconnect transitions join that run. An explicit refresh joining a reconnect during its health phase promotes the run to perform the required Root refresh. If the reconnect has already crossed that phase without invalidation, a later explicit refresh starts a new generation rather than losing mutation-refresh semantics. A terminal-failure confirmation probe may also be adopted by an overlapping replacement generation for the same complete tab identity; each consumer retains its own generation guard, so the shared result cannot cross tab replacement or commit through a retired generation. Tab removal or identity replacement retires both join owners; distinct tabs never share work. No timer or optimistic connection fact is added.
- Task projection law (2026-07-16): Public task facts are `trackedTaskProgress`, `documentChecklistSummary`, and `applyInstructionProgress`; the generic task `progress`, `TaskProgress`, and `TaskProjection` contracts are removed without aliases. `trackedTaskProgress` selects the artifact whose output equals `apply.tracks` (or the `tasks` artifact when Apply omits `tracks`), expands only that output glob, and falls back only to top-level `tasks.md` when schema/artifact resolution or matched source fails. It alone drives global task counts, workflow phase, completion notifications, and task-status labels; `0/0` is `no-tasks`, never complete, and no derived task statistic claims archive readiness.
- `documentChecklistSummary` deduplicates physical Markdown files matched by schema artifacts, retains every matching artifact id per file, and groups tasks/counts by file. When entity metadata exists but schema detail is unavailable, it may retain all Markdown files as explicitly secondary analytics. It never drives readiness, notifications, primary progress, or workflow state.
- `applyInstructionProgress` preserves the literal counts and state from `openspec instructions apply --json` under an attributed source. Raw command adapters may retain upstream's field name inside CLI evidence, but application contracts do not expose it as generic task progress. When Apply differs from tracked glob truth, both values remain unchanged and the Change surface displays a non-fatal upstream-divergence diagnostic.
- Decision (2026-07-15): Live and static modes share one source-aware Spec Catalog. Spec identity is compound: owned Specs use `(owned, specId)` and referenced Specs use `(referenced, storeId, specId)`. Routes, cache keys, search records, provider lookups, and SSG enumeration must preserve that full identity; never treat `specId` as globally unique.
- Static export supports only direct Reference Specs declared by the active CLI-resolved planning root. Materialize them through official `openspec list --specs --store <id> --json` and `openspec show <spec-id> --type spec --store <id>` paths; never walk transitive References, read the registry directly, or export referenced changes, archives, config, Git state, and unrelated Stores.
- Decision (2026-07-15): When effective References exist, static export requires `--references=include|omit`. Missing policy stops the export. `include` requires complete successful materialization and must not publish partial output; `omit` retains an explicit omission state rather than pretending no References exist.
- Static Reference snapshots retain compound Spec identity, Store id for included content, source/read-only state, and observation time. Remove absolute paths (including `meta.projectDir`), registry/data-home paths, remote URLs, backend/environment identity, and raw path-bearing diagnostics. An omitted export exposes only the omission state and aggregate count, not unpublished Store identities.
- Decision (2026-07-15): The OpenSpec 1.6 Wayfinder map is complete. Formal implementation truth now lives in `openspec/changes/target-openspec-cli-16-line/loop/{intake,research-plan,implementation,checkpoints}.md` under the declared `opsx-collab-pr-loop` schema. Keep `wayfinder/` as decision provenance; do not maintain it as a parallel execution plan.
- The repository-local OpenSpec artifact CLI currently reports 1.5.0 while the product target and `references/openspec` authority are 1.6.0. Use the local CLI to follow the declared artifact schema, but verify product behavior against the pinned 1.6 source, executable contracts, JSON, and exit status.
- Implementation baseline (2026-07-15): Section 2 CLI 1.6 contract baseline is complete at 11/11 on `feat/openspec-cli-16-contract-baseline`. Typed CLI evidence, 1.4-1.6 workflow/tool regressions, 6.x compatibility guidance, strict validate/archive behavior, Update/Sync Web actions, multiline Requirement rendering, and scenario-loss diagnostics pass all required local gates in an isolated clean worktree. The slice is not merged; do not claim Section 3+ production contracts from parallel skeleton files.
- A typed CLI command result preserves parsed command data, the raw JSON document, stdout, stderr, structured diagnostics, process success, and exit status as separate facts. Contract validation may report missing required semantics but must not discard raw evidence; additive fields remain accepted.
- First-party executable evidence against `references/openspec@e1b51d1` covers nearest/declared/explicit roots, self/missing/direct References, empty healthy Stores, multiline Requirements, proposal-less nested validation failure, and scenario-loss archive failure. Keep the source/tag regression tests alongside adapter fixtures so the version gate cannot substitute for feature completeness. Fixtures must not synthesize optional fields absent from the official command payload; an invented superset is not evidence that a real CLI projection works.
- Frontend handoff (2026-07-16): ClaudeCode's uncommitted App/Web skeleton is now owned by this implementation loop. The Web Context placeholder has been replaced by the public Root Context subscription; App environment/Store placeholders remain intentionally pending the hosted protocol in Sections 8-9. Preserve the shared-worktree files and do not claim skeleton routes as completed checkpoints without real data and tests.
- Independent-review baseline (2026-07-16): The `8b81f7d...8d38f35` blockers now have recurrence-safe working-tree corrections: one physical/reactive writer, serialized Planning-root replacement, typed per-Reference Store enumeration, and dedicated public OpenSpec execution with one strict Archive owner. Re-close only the explicitly reopened checkpoints after focused and full local evidence, push PR #207, and stop for another independent review; do not treat implementation evidence as merge approval.
- Projection Work loading law (2026-07-23): Live projections use a Server-owned Work identity containing projection kind, CLI-selected Planning-root identity/source, effective Store selector, owner generation or Git binding token, explicit selector, input fingerprint or invalidation generation, and protocol version. Same-identity subscribers share one bounded in-flight Work and may replay a provenance-bearing display snapshot, but stale, reconnecting, or failed data never authorizes mutation and retired A work cannot publish into B. Work emits real phase, batch, completion, and error evidence; `queue-enter` and `resource-admitted` bound scheduler wait separately from leaf execution, while the enclosing Planning-root span owns pre-Work Root admission. Dashboard Summary/Git/trends/workflow, Changes rows, Root Context, and OPSX Status are independently owned projections. Status must not wait for full Kernel warmup, and hashing/persistence/Workers require measured ROI and bounded resource budgets. Final browser acceptance remains owner-only.
- Root snapshot settlement law (2026-07-23): Only reactive operations may write reusable Root Context snapshots; imperative queries, mutations, and PTY operations always re-confirm CLI selection and cannot seed later reactive replay. A successful launch Project Binding physical/reactive write invalidates `project/context` before its detached preview returns, so a subscriber cannot combine binding B with leased Root A. The public subscription keeps A display-only until A drains, then publishes B; this is a lifecycle/provenance boundary, not a loading-state preference.
- Retained Pull admission law (2026-07-27): A typed Pull that only returns bounded Server-retained/current projection state is a reactive display operation. It must execute through the current reactive Manager/Root snapshot and must not acquire an imperative lease that repeats OpenSpec Doctor/Context after the subscription already resolved that Root. Imperative mutations, workflow commands, terminals, and PTYs still re-confirm the current Root before acting. Performance evidence must distinguish fresh-Server cold computation from a fresh browser Document admitted against the same Server; improving the latter never proves the former is fast.
- Realtime visual projection law (2026-07-23): Every current Web route and overlay derives wait presentation from one push-to-pull projection lifecycle rather than a boolean `isLoading`. The visual topology is no-content `idle | initial-loading | empty | initial-error` and content `partial | current | revalidating | refresh-error`; `current | display-only` authority and `initial | server-push | user-action | reconnect | root-rebind` cause remain orthogonal facts. A server push invalidates only a projection identity; only a current client pull may commit content or trigger an update effect. Normal lifecycle is a low-noise visual language of shape, luminance, and native motion, with reduced-motion and hidden accessible equivalents; errors, empty conclusions, blocks, and conflicts retain concise text and actions. Retained display-only content remains readable but keeps current-dependent mutations locked. Do not alter page information architecture or the future Kanban layout work in this slice.
- Delivery-correction law (updated 2026-07-24): A reviewed OpenSpec Change with unchecked tracker work, stale PR checks, a failing local gate, or owner-only acceptance cannot be represented as complete. A replacement Change does not close it by assertion: reconcile each old task with linked proof or an explicit transfer. OpenSpec may archive an intentionally partial/superseded Change after manager confirmation; preserve unchecked tasks and warnings, record the successor, assess delta-spec sync, and never convert that archive into a completion claim. For hosted App contracts, `envUri` is Server-issued from stable host identity plus effective data home; every connection surface consumes it rather than deriving a path surrogate. Gate 401/403 is `authentication-required`, not offline or mutation `indeterminate`; credentials are session-only and locator-scoped; environment mutations require explicit selected current backend. Store start exposes actual `accepted -> running -> terminal` observation through a Server-owned ledger, while inventories remain push-invalidation/pull projections. Every public HTTP/WS/App boundary needs a real-owner red/green/mutation-resistance test before broad gates.
- Hosted browser Access Gate delivery law (2026-07-24): Server-side rejection evidence does not prove that Direct Web or App-embedded Project Web can use a guarded backend. CLI launch must privately carry the exact resolved credential to the Direct/App fragment; App forwards it only to the matching locator iframe; Project Web consumes and removes it before rendering and keeps it in one in-memory owner. Immutable shell entry/assets may load without data authority, while API, tRPC HTTP, tRPC WS connection params, PTY auth-first, file/preview/sound/notification data, and raw protected fetch/resource paths remain gated. Selected App authority is exact tab identity plus current observation generation, never URL equality alone. Root errors and direct Reference provenance remain source-labelled; `error` must not be rewritten as ordinary stale state.
- Project Web authentication admission law (2026-07-27): A live Project Web document consumes and strips its
  transient credential, then completes one protected backend admission before importing App-owned navigation,
  tRPC, WebSocket, or PTY transports. `200` starts the normal App; `401/403` renders an explicit terminal
  `authentication-required` document and starts no ordinary transport/retry loop. Network/5xx remains a concrete
  recoverable connection failure, and static mode performs no live admission. Do not patch each Loading surface or
  hide downstream unauthorized errors independently.
- Embedded capability delegation law (2026-07-27): The App-owned Project Web iframe delegates only capabilities
  required by the embedded workflow. Terminal Clipboard requires `clipboard-read; clipboard-write`; do not add
  camera, microphone, display capture, filesystem, unrelated features, or wildcard delegation. Unit tests prove the
  exact iframe attribute; final browser capability behavior remains manager-owned acceptance.
- App continuity and containment law (2026-07-27): SessionTabs status and iframe treatment consume the same accepted
  locator/observation generation; never add polling or a second iframe-reported connection truth to repair visual
  lag. Explicit focus/visibility refresh and an established transport failure share one in-flight health probe for
  the same tab observation generation; neither source may start a duplicate request. Objective offline/authentication
  publishes immediately, while HTTP success during WebSocket reconnection remains checking and cannot fabricate
  Live. Focus/visibility refresh retains settled Inspector content, selection, and component
  identity while replacement data updates. The stable selected locator is read provenance only; every Store write
  still requires current selected-tab/generation authority. A resolved HTTP mutation admission may project its exact
  accepted envelope until the WebSocket ledger carries that request, but only ledger terminal settlement refreshes
  Store/Context and Inventory is never changed optimistically. Persistent Sessions consumes the App shell's remaining
  block size, and Inspector long facts reflow within the available inline size; neither correction may remount the
  iframe or redesign navigation.
- App observation provenance law (2026-07-24): Starting a replacement observation generation may make retained Root/Reference evidence display-only, but may not relabel that evidence with the new generation, new health/envUri, or a new observation timestamp. Retained evidence keeps the exact source identity that produced it until replacement Root data commits. A retained evidence record and the current Root attempt are non-interchangeable facts: each carries its own full tab identity, generation, health/envUri when known, status/error, and observation time. The UI may show A evidence beside B failure, but it must neither attach B failure to A provenance nor hide B merely to preserve A. Mutation authority is an atomic correlation of full tab identity (id, locator, session, creation identity) and its current observation generation; an old observation plus a same-id/same-locator replacement tab is never valid authority, including before React effects retire it. Guard evidence must reach the real mutation owner: mutating a committed disabled DOM property, rendering internal generation fields solely for tests, or allowing a missing route-owned mutation callback is not proof.
- App iframe continuity law (2026-07-26): A same-tab background health revalidation immediately retires mutation authority (`current=false`) and starts a source-distinct attempt, but retains the last compatible health payload, including opaque `embeddedUiUrl`, as display-only evidence. `rootAttempt.health` remains null until the replacement probe succeeds. Checking, offline, and transport-failure transitions must not conditionally unmount an already rendered Project Web iframe; a changed authoritative entry, incompatible/authentication-required admission state, explicit reload, or tab identity retirement remains allowed to replace it. Regression evidence must suspend the real replacement health probe, assert the same iframe DOM node remains mounted, and fail when the exact retained-health transfer is removed; a stable `src` string assertion alone is insufficient.
- Hosted worker/CLI owner law (2026-07-24): A bundled `!isMainThread` check is not a worker protocol discriminator. Worktree Server and managed-translation workers use typed kind envelopes, and each handler validates/executes only its own kind; a process fallback or launch-plan helper cannot prove the default production worker. Browser Gate delivery evidence must cross the actual CLI start-command callback into the Direct/App browser target it requests. A test that obtains the callback credential and manually calls URL builders does not prove that owner. Resource-worker evidence includes a non-empty retired initiating-client id, not only an empty id.
- Hosted presentation owner law (updated 2026-07-31): Server readiness produces one semantic `project-web | hosted-app` presentation request containing runtime-only credential evidence. The selected presenter owns materialization: Browser builds the private URL and uses one same-origin Browser relay plus best-effort source retirement; OpenTray may show/focus one retained native window. The coordinator must not depend on `openBrowser(string)`, log/persist the private target, or claim portable guaranteed focus for ordinary browser tabs. A forwarded source retires after Browser acknowledgement; a locally applied or fallback-applied source remains mounted. PWA installation, role priority, and Web App Launch Handler ownership are forbidden.
- OpenTray App daemon law (updated 2026-07-30): OpenTray is the native presenter of one user-level App daemon, not a second App, reverse proxy, or competing persistence model. Bare `openspecui [project]` remains foreground `serve`. Every externally invoked `serve` owns its project backend and registers a reconnecting presentation lease over user-isolated IPC; the daemon may own project children only when its authenticated local Workspace Home explicitly starts a canonical directory. Native presentation creates one retained `@opentray/ext-webview` session with `style.appMode: true`; later activation uses `toVisible()`/`focus()` without replaying bootstrap options. Native runtime options persist a shell-free `appLaunch` vector that re-enters the public `openspecui start` lifecycle: source execution explicitly restores the development package condition, retains only its absolute TypeScript-loader arguments, and rejects transient Node flags; packaged execution uses the absolute `dist/cli.mjs`. Never persist the private detached-daemon bootstrap. The WebView extension owns warm Dock reopen for the most-recently-active retained appMode window, so the consumer must not install a duplicate reopen listener. Daemon Web mode publishes no `appLaunch` and may mount base tray/browser behavior but must not import or initialize the WebView extension. `--app` and `--web` on the bare command are aliases for `serve --app` and `serve --web`.
- OpenTray presenter boundary law (updated 2026-07-31): `opentray` and `@opentray/ext-webview` stay on one exact protocol line and remain external runtime facades; platform packages enter only through their optional dependency closures. The CLI requires the native App bootstrap URL itself to be IPv4-loopback HTTP. OpenTray 0.18.0 classifies loopback as `local` but cannot match its exact origin, so the Window capability temporarily uses the `"'local'"` source selector to obtain overlay/drag bridge injection; this is an explicit compatibility concession, not exact-origin evidence. Wildcard, screen, tray, and global-binding authority remain forbidden. Restore exact daemon-origin authority when the upstream matcher can preserve both locality and origin. A failed/unsupported native presenter resolves to effective `web` status with structured, target-free diagnostics. Tray Quit is daemon Stop: it must retire listeners and App HTTP before bounded WebView/tray destruction, and a pending presentation RPC cannot retain daemon or managed-backend lifetime. Required same-origin daemon control returning HTML or an older contract is an explicit restart-required state; raw parser errors and simultaneous offline/waiting claims are forbidden.
- App daemon admission law (updated 2026-07-30): When no daemon exists, interactive `serve` with no explicit presenter asks `Start OpenSpecUI App? [Y/n]`; non-TTY execution keeps Direct Web. `serve --app` may start the daemon, while `serve --web` uses Direct Web when absent and attaches plus opens Direct Web when present. `--no-open` performs no prompt, daemon start, Workspace dispatch, or browser opening. Daemon startup mode is immutable for its lifetime; an explicitly conflicting `start` request reports the exact `restart` correction rather than mutating tray/window behavior. Daemon `stop` settles only daemon-managed project children and never externally owned foreground Servers. Daemon `restart` captures its exact managed directory set, settles those children, replaces the daemon, and restores each once; live external `serve` leases re-register normally.
- Local App shell law (2026-07-29): The daemon serves App assets built and packed with the same CLI release on a loopback endpoint. Public `appBaseUrl`, Settings Hosted App URL, and `--app=<url>` are removed; a separately deployed App may remain manually usable but is not a local daemon dependency. Each backend keeps its own port and existing HTTP/RPC/WebSocket/PTY contract. App development may inject a repository-owned dev source, but product runtime does not expose a shell-location setting. The same-origin daemon control transport follows `Push invalidation -> Pull complete snapshot`; runtime credentials bind to their backend locator in App memory before credential-free tab application and are never cached or persisted. A separately hosted/manual App treats an absent daemon endpoint as unsupported, while an objective local control failure remains visible.
- CLI App asset-projection law (2026-07-30): `@openspecui/app` build and watch-build are the sole owners that replace the complete `packages/cli/app` runtime tree. Source CLI and packaged CLI both serve that same physical tree; neither resolves `packages/app/dist` directly. CLI execution, including `pnpm openspecui`, never triggers an implicit App build. CLI/package build orchestration may request the App build, but it must not implement a second copy owner or retain `build:copy-app` glue.
- Workspace presentation law (updated 2026-07-30): App product language and navigation use `Workspaces`, replacing `Sessions` while preserving multi-backend iframe continuity, reachability, tab persistence, and runtime-only credential ownership. The first tab is fixed Workspace Home and cannot close or reorder; project tabs follow it. A project title uses a verified GitHub `org/repo` slug when available, otherwise the canonical directory basename; current Git branch is subtitle, complete path is retrievable, and host/port remains diagnostic-only. Each project Workspace exposes a tooltip-labelled Open in browser icon that dispatches its opaque Workspace id to the daemon; only the daemon resolves and opens that registered backend's credential-safe Direct Project Web target. Tab trigger, Open in browser, and Close are sibling native controls; nested `role=button` imitations are forbidden. Closing a tab never implies backend Stop. The tab strip is the only narrow-width inline scroll owner, while the App route and shell remain page-contained. Arbitrary external URLs and persisted/logged credentials are forbidden.
- App titlebar and initial-placement host law (updated 2026-07-31): The App shell may run in an ordinary Browser document or an OpenTray window, and exactly one `browser | opentray | native-frame` titlebar-geometry source owns its inset at a time. Browser owns no artificial titlebar. A macOS native presenter declares `appMode=opentray-overlay` in its private initial App URL; App bootstrap captures that declaration in Router Context before navigation, so the global `AppLayout` renders its self-drawn titlebar even when native bridge injection or geometry measurement is late. The declaration decides chrome presence only; `navigator.opentrayWindow.overlay` remains the geometry owner and `startAppRegionDrag()` remains the drag authority. Retired listeners are removed, and late async geometry cannot overwrite a replacement source. OpenTray overlay renders one visually explicit, full-width, fixed-32px application chrome row before every routed surface. Native geometry affects horizontal padding only: before measurement each edge has an 8px fallback, and every positive control inset receives another 4px content margin; the reported overlay height must never expand the titlebar. The titlebar owns the branded product mark and the sole overlay Settings entry at its right edge; Browser and Windows native-frame hosts retain navigation Settings as fallback instead of rendering artificial chrome. Blank non-interactive titlebar surface is the only OpenTray native-drag entry; Settings, Workspace tabs, open-in-browser, close, inputs, and actions never inherit caption/drag semantics. Titlebar, sidebar brand, OpenTray App identity, and tray identity derive from the same App-owned logo source; each runtime receives its platform-correct format, and the macOS tray candidate remains a template icon. The retained native window applies `WebviewPlacementKit` `screen-center` exactly once after its first `show()`; later activation never resets the user's moved position. `pnpm openspecui` source execution declares `devtools: true`, opens DevTools after the first `show()`, and treats open failure as non-fatal diagnostic evidence; packaged `dist` execution never enables DevTools. PWA install prompts, manifests, service-worker cache/update, standalone launch priority, Launch Handler consumption, and PWA overlay geometry are forbidden.
- Native bridge and App-icon build law (2026-07-30): A host-declared OpenTray overlay resolves the window bridge through `navigator.opentrayWindow`, compatible `navigator.window`, or `navigator.opentray.window`. Absence on first React mount is pending, not Browser truth: one bounded retry owner must converge to measured geometry or retire, and teardown must clear both retry and geometry subscriptions. Once that declaration and an overlay bridge exist, `getTitlebarAreaRect()` is authoritative even when the overlay's `visible` hint is false; that hint must never gate control-safe measurement. Permanent fallback padding is invalid because it allows native controls to overlap App content. `openTrayAppIconPlugin` is the sole Vite dev/build owner of App identity ICNS, ICO, Linux PNGs, preview, and relocatable manifest from the canonical App brand symbol; CLI runtime consumes those generated paths and never owns a hand-authored parallel App-icon implementation. Tray artwork remains independent because template rendering has a different platform contract.
- App Workspaces/Stores information-architecture law (updated 2026-07-31): The App exposes exactly Workspaces and Stores as primary domain destinations; Settings remains a secondary utility. Connections are credential-free candidates inside the Workspace Launcher opened by `+`, while open Workspaces retain distinct tab/session/frame identity. Daemon candidates remain runtime-only, new daemon admission may auto-open once, closing a Workspace does not forget its candidate, and an unchanged daemon snapshot cannot reopen it. Stores is the user-facing entry, but Environment remains its opaque registry/authority scope: every Store UI identity is `(envUri, Store id)`, backend locators are internal current access authorities, and same-id Stores never merge across Environments. Store Inventory becomes the index, Inspector becomes Store Detail, and Context Matrix becomes observed-only Usage. Store Detail owns governance plus readonly Spec/active-Change summaries; editing, Apply, Archive, and duplicate Project Web behavior remain Workspace responsibilities. Errors, authority loss, source conflict, and mutation failures stay direct; healthy paths, Git/metadata, provenance, and raw CLI evidence remain secondary. Delivered (reshape-app-workspaces-and-stores): `app-router.tsx` redirects `/` to `/workspaces` and registers `/stores`, `/stores/environments`, and `/stores/$encodedEnvUri/$storeId`; Task Manager is an App-owned Dialog rather than a route, and the Connections/Environment/Store-Manager routes plus `store-manager-backend-selector.tsx` are retired without compatibility glue. The credential-free Workspace candidate catalog (`workspace-candidate-catalog.ts`) and Environment authority owner (`environment-authority.ts`) are separate from open-Workspace tab identity; the Store mutation dispatch boundary composes the Environment authority gate with the connection-observation authority gate (`store-action-environment-authority.ts`). The daemon owns managed directory launch (`managed-project-owner.ts`, `daemon-protocol.ts` `start/stop-managed-project`); an unchanged snapshot cannot reopen a closed Workspace (`daemon-workspace-admission.ts` reducer drives `AppDaemonWorkspaceOwner`).
- Path-first Workspace management law (updated 2026-07-31): Workspace Home is the fixed first tab and owns Favorites, one local path-input launch form, Recent directories, and the Task Manager Dialog entry. Workspaces secondary navigation directly lists favorite canonical directories without a `Running`/`Favorites` accordion; selecting one focuses its current Workspace or starts it through managed daemon authority. Secondary rows never use active-route highlighting: the only persistent status affordance is a right-edge signal driven by verified Running evidence, with pending launch distinguished transiently. Canonical physical directory is the durable managed-service/catalog identity; symlink/input aliases must converge before spawn. Favorites and successful recency persist only canonical credential-free path facts, independent of running/open state. The local daemon starts at most one fixed-plan managed child per physical directory. Closing a project tab preserves that service; explicit Stop retires its exact generation. Task Manager lists every daemon registration with path-first display, owner, start time, lifecycle, and independent runtime evidence. A registration is Running only after a compatible Health API result and an established WebSocket subscription for that exact backend; WebSocket loss immediately retires Running. Managed rows expose exact-generation Stop. External rows without a callable owner-shutdown channel expose no Close, Remove, Delete, or Stop lifecycle action; manual Connection candidate Forget/Remove remains Launcher-owned. GitHub `org/repo` and branch are objective display facts, never clone inputs or identities. Credentials, backend URL, port, PID, private fragments, and generation authority never enter directory history/favorites.
- OpenTray App chrome law (2026-07-31): The self-drawn OpenTray overlay titlebar is the native window's sole direct brand header and displays the exact product name `OpenSpecUI App`; the desktop sidebar hides its duplicate brand block only in that presentation. Browser/native-frame presentation retains the sidebar brand because it has no App-owned overlay titlebar. The titlebar brand and inline sidebar brand are two controls over one AppLayout-owned expansion state. Collapsing creates a compact primary-navigation rail: the inline brand retains only its logo, primary destinations retain only icons with accessible names/tooltips, and secondary Workspace navigation plus its feedback leave the rendered desktop sidebar. The rail clips overflow for the complete width transition; secondary content exits before collapse and re-enters only after expansion settles, so transient width cannot create a horizontal or vertical scroll owner. Window role, overlay geometry, brand placement, sidebar expansion, and navigation selection remain orthogonal facts.
- Workspace directory catalog and local-control law (updated 2026-07-31): The App daemon is the sole user-level Favorites/Recent owner and atomically persists `$OPENSPECUI_HOME/workspace-directory-catalog.json`; browser `localStorage`, document state, and storage events are forbidden as catalog authority. Workspace Home, Task Manager, and every App window consume revisioned replacement snapshots through daemon invalidation Push -> Pull. Favoriting a current canonical directory that has no history record creates the credential-free directory entry; only a successfully ready/admitted managed start advances recency, while a failed start never does. The file and wire contract contain only canonical path, favorite, and recency, never credentials, backend URL, port, PID, generation, or private fragments. Browser start, Stop, Open, and favorite controls are accepted only from the bundled local App's exact loopback origin (and same-origin Fetch context when supplied) before any managed or persistence owner is invoked. A local endpoint, loopback host, or valid JSON body without exact-origin evidence is insufficient authority. Project Web CORS admits syntactically valid HTTP(S) loopback origins so an ephemeral local App origin can observe external foreground backends; non-loopback browser origins remain explicitly allowlisted.
- Environment Store-source and cleanup law (updated 2026-07-31): Every current compatible source independently Pulls and settles its own Store list and Doctor projection before same-Environment evidence can be compared. One deterministic source remains the stable read source while current. A settled source conflict preserves that source and all source-labelled readonly evidence but freezes mutation authority; it must not blank the index/detail or silently switch evidence. Store routes encode opaque `envUri` as versioned `v1-<UTF-8 hex>` and decode it only through the typed route helper. `unregister` removes registry membership while preserving checkout files; `remove` removes registry membership and deletes the checkout. Cleanup closes only after a Server-ledger terminal record matches the exact `(requestId, envUri, Store id, kind)` tuple. External backends currently have no callable App shutdown channel, so Task Manager exposes objective ownership/runtime evidence but no fake lifecycle command.
- Foreground Server shutdown law (2026-07-30): One `SIGINT` or `SIGTERM` must settle the foreground `serve` owner without requiring cooperation from connected clients. Server shutdown actively retires every Server-owned tRPC and PTY WebSocket client, waits for WebSocket and Node HTTP close settlement, and disposes both the launch-project `CliExecutor` and every Planning-root executor so buffered OpenSpec commands cannot outlive their Server. Successful bounded CLI probes must clear their timeout handles; a settled probe timer is not allowed to retain the process until its nominal deadline. Focused evidence must cross the production owners with a withheld real WebSocket, a non-cooperative buffered child, and exact timeout-handle cleanup, then prove an installed packed CLI exits after one signal.
- Buffered CLI admission law (2026-07-31): `Projection Work` resource concurrency limits Work identities, not subprocesses created inside one Work. Every Root-resolution and projection-owned buffered OpenSpec invocation therefore crosses one `CliExecutor`-local FIFO child-admission boundary; streaming Terminal commands remain independent. Aggregate Status, Schema, and referenced-Spec projections must submit entity calls lazily and preserve source order instead of using eager `Promise.all`, otherwise one aggregate can occupy the whole queue before a later Root/Detail request arrives. OTel must separate `cli.execute` queue wait from `cli.process` spawn-to-output time through `cli.admission.queued/acquired`, and retain exact cwd, resolved runner realpath, parent PID, system load, admission depth, and real unsettled-child concurrency. A concurrency regression must drive real child processes and fail when two buffered children overlap.
- Buffered OpenSpec execution-backend law (2026-07-31): `CliExecutor` defaults buffered OpenSpec commands to `worker`; `OPENSPEC_SPAWN_MODE=process` remains the explicit diagnostic/compatibility override, and invalid values are rejected. Worker resolution targets the installed package's `dist/cli/index.js`, prepares isolated argv/env plus the physical project cwd before import, invokes exported `program.parseAsync`, and projects stdout/stderr/exit/cancellation through the same typed result. Node-bin and Vite+ shim installations are supported explicitly. Only objective module absence (`ENOENT`/`ENOTDIR` across every supported location) permits a process fallback. Permission errors, malformed modules, import failures, missing exports, and CLI execution failures remain Worker failures and never rerun the command in a process. Buffered disposal terminates and awaits active Workers. Streaming Terminal commands remain real child processes.
- Daemon-frozen OpenSpec execution law (2026-07-31): The detached App daemon samples the effective OpenSpec execution mode once at startup, defaults it to `worker`, publishes that immutable `process|worker` fact in daemon status, and uses it for every daemon-managed backend hosted in that process. A later CLI invocation cannot mutate an already-running daemon's environment: `start` rejects an execution-mode mismatch with the exact restart command, while `restart` preserves the managed directory set and starts the replacement under the requested mode. Externally owned foreground `serve` processes keep their own invocation environment. OTel names the effective backend Span and lifecycle events `cli.process.*` or `cli.worker.*`, records requested/effective mode on response and backend Spans, records `module-not-found` when default Worker falls back, records the resolved module only for Worker execution, and never fabricates a child PID for a Worker.
- Core child-ownership evidence law (2026-07-17): `CliExecutor` owns its direct streaming child through a non-root internal slot. Forced termination may settle as an immutable failure while that slot remains occupied; only the actual Core `close`/pre-spawn-error transition releases it. A late-close regression test must assert the slot moves from its spawned child to empty exactly once after two close events, and must fail when that release transition is removed. Do not expose this inspection seam through the package root, `CliStreamHandle`, diagnostics, or generic CLI execution.
- Execute `loop/checkpoints.md` in order and continuously synchronize actual progress, verification evidence, and approved divergences into `loop/implementation.md`. Any listed loopback trigger requires manager re-approval before continuing.
