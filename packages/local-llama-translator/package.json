{
  "name": "@openspecui/local-llama-translator",
  "private": true,
  "version": "11.1.0",
  "description": "Server-side llama.cpp translator engine for OpenSpecUI",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": "./dist/index.mjs",
      "default": "./dist/index.mjs",
      "types": "./dist/index.d.mts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "// runtime-note": "tsdown keeps the dynamic import for node-llama-cpp in the built output instead of bundling the heavyweight native runtime into this package.",
  "// host-note": "The runtime host packages (openspecui / @openspecui/server) and this adapter declare node-llama-cpp as an optional peer. Keep this range aligned with the dynamic source import; end users install it only through local-llama runtime admission.",
  "dependencies": {
    "@openspecui/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "node-llama-cpp": "~3.18.1",
    "tsdown": "^0.16.6",
    "typescript": "^5.7.2",
    "vitest": "^4.1.0"
  },
  "peerDependencies": {
    "node-llama-cpp": "~3.18.1",
    "typescript": "^5.0.0"
  },
  "peerDependenciesMeta": {
    "node-llama-cpp": {
      "optional": true
    }
  }
}
