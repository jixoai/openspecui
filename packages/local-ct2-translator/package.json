{
  "name": "@openspecui/local-ct2-translator",
  "private": true,
  "version": "8.0.0",
  "description": "Server-side CTranslate2 translator engine for OpenSpecUI",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": "./dist/index.mjs",
      "default": "./dist/index.mjs",
      "types": "./dist/index.d.mts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "smoke": "tsx scripts/smoke.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "// runtime-note": "tsdown keeps the dynamic import for ctranslate2 in the built output instead of bundling the native runtime into this package.",
  "// host-note": "The runtime host packages (openspecui / @openspecui/server) and this adapter declare ctranslate2 as an optional peer. Keep this range aligned with the dynamic source import; end users install it only through local-ct2 runtime admission.",
  "dependencies": {
    "@openspecui/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ctranslate2": "^1.0.0",
    "tsdown": "^0.16.6",
    "typescript": "^5.7.2",
    "vitest": "^4.1.0"
  },
  "peerDependencies": {
    "ctranslate2": "^1.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependenciesMeta": {
    "ctranslate2": {
      "optional": true
    }
  }
}
